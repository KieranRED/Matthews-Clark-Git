---
phase: 05-integration-catalogue
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - scripts/build-catalogue.mjs
  - scripts/validate-catalogue.mjs
  - public/wrap-studio/catalogue.js
files_modified_note: "Overwrites public/wrap-studio/catalogue.js created in Plan 01"
autonomous: true
requirements: [CAT-01, CAT-05, CAT-06]
user_setup:
  - service: vercel-blob
    why: "Upload 18 STEK swatch PNGs to Vercel Blob CDN for swatch imagery (CAT-06)"
    env_vars:
      - name: BLOB_READ_WRITE_TOKEN
        source: "Already present in .env.local — no action needed"
must_haves:
  truths:
    - "All 375 real wrap films appear in window.WRAP_CATALOGUE"
    - "Each entry has real code, series, finish, and brand display name"
    - "18 STEK swatches have a Vercel Blob swatchUrl; the other 357 have swatchUrl: null"
    - "Entries retain finish: 'metallic' and finish: 'carbon' as first-class keys"
    - "72 low-confidence entries carry hexConfidence: 'low'"
  artifacts:
    - path: "scripts/build-catalogue.mjs"
      provides: "ESM build script: reads wrap-colours.json, uploads STEK swatches, writes catalogue.js"
      contains: "from '@vercel/blob'"
    - path: "scripts/validate-catalogue.mjs"
      provides: "Validator asserting 375 entries, field completeness, 18 blob URLs"
    - path: "public/wrap-studio/catalogue.js"
      provides: "Generated IIFE assigning window.WRAP_CATALOGUE (375), window.FINISHES (9), window.BRANDS (3)"
      contains: "window.WRAP_CATALOGUE"
  key_links:
    - from: "scripts/build-catalogue.mjs"
      to: "public/wrap-studio/catalogue.js"
      via: "writeFileSync of generated IIFE"
      pattern: "window.WRAP_CATALOGUE"
    - from: "scripts/build-catalogue.mjs"
      to: "Vercel Blob"
      via: "put() of 18 STEK PNGs"
      pattern: "put\\("
---

<objective>
Write the Node ESM build script that converts the real 375-entry `wrap-colours.json` into the prototype's `catalogue.js` schema, uploads the 18 STEK swatch PNGs to Vercel Blob, and writes `public/wrap-studio/catalogue.js` (replacing Plan 01's demo copy). Also write a validator and run both. After this plan the studio shows all 375 real films with real codes/series/finishes and STEK swatch imagery.

Purpose: CAT-01 (375 films), CAT-05 (real code/series/finish), CAT-06 (swatch imagery from blob).
Output: `scripts/build-catalogue.mjs`, `scripts/validate-catalogue.mjs`, regenerated `public/wrap-studio/catalogue.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-integration-catalogue/05-RESEARCH.md
@.planning/phases/05-integration-catalogue/05-UI-SPEC.md

<facts>
- Project is ESM. scripts/ already holds `.mjs` files (e.g. backfill-clients.mjs) — name new scripts `.mjs`, use `import` syntax.
- @vercel/blob@2.3.3 installed. BLOB_READ_WRITE_TOKEN in .env.local. Run with: `node --env-file=.env.local scripts/build-catalogue.mjs`
- Source data: `/Users/kieranredpath/Downloads/Wrap colours/Extract/wrap-colours.json` — top-level ARRAY of 375 entries.
- Swatch PNGs: `/Users/kieranredpath/Downloads/Wrap colours/Extract/swatches/stek/{colour-shift,gloss,matte}/*.png` — exactly 18 files total (carbon/ and ppf-colour/ subfolders are EMPTY). PNG stems exactly match entry `id` (verified 18/18).
- Real entry fields: id, brand (avery|hexis|stek), series, name, code, finish, hex, rgb, notes, [hex_confidence].
- Prototype entry fields needed: id, brand (DISPLAY name), series, name, code, finish (prototype key), hex, hex2, tier, thickness, conform, warranty, proTip, swatchUrl, hexConfidence.

FINISH MAPPING (raw → prototype key):
  gloss→gloss, satin→satin, matte→matte, chrome→chrome,
  colour-shift→shift, metallic→metallic (KEEP first-class), brushed→satin,
  carbon→carbon (KEEP first-class), ppf-clear→ppf-clear, ppf-matte→ppf-matte,
  ppf-colour → id.includes('matte') ? 'ppf-matte' : 'ppf-clear'

BRAND DISPLAY MAPPING: avery→'Avery Dennison', hexis→'Hexis', stek→'STEK'

TIER INFERENCE (customer never sees it, but app internals use it):
  chrome|colour-shift → specialist
  stek + ppf-colour → specialist
  stek + carbon → specialist
  metallic|brushed → premium
  matte → premium
  else (gloss/satin solids) → standard

hex2: null for all (real data has no second colour).
hexConfidence: copy raw hex_confidence if present (72 'low'), else null.
proTip: use entry.notes if non-empty; else finish-based fallback:
  matte: "Matte films show every prep imperfection. We decontaminate and inspect before a single panel is laid."
  chrome: "Chrome needs a primer on the edges and is unforgiving on complex curves. Full-car chrome is a specialist job — we quote it honestly."
  shift: "Colour-shift reads differently from every angle. Bring the car in to see the full flip before you commit."
  ppf-clear/ppf-matte (STEK colour PPF): "Colour PPF is protection and colour change in one film — thicker and more durable than vinyl, priced to match."
  others: '' (empty — panel handles empty proTip).
thickness/conform/warranty: SPEC lookup by prototype finish. Use reasonable static values per finish group (e.g. gloss/satin/matte vinyl: thickness '~80 µm', conform 'High', warranty '5–7 yr'; chrome: '~100 µm', 'Medium', '3 yr'; shift: '~90 µm', 'Medium', '5 yr'; ppf-clear/ppf-matte: '200 µm', 'High', '10 yr'; metallic/carbon: '~90 µm', 'High', '5–7 yr'). These are informational chips only.

OUTPUT catalogue.js shape (IIFE):
  (function(){ const C = [ ...375 entries... ];
    window.FINISHES = [ {key:'gloss',label:'Gloss'},{key:'satin',label:'Satin'},{key:'matte',label:'Matte'},{key:'metallic',label:'Metallic'},{key:'chrome',label:'Chrome'},{key:'shift',label:'Colour-shift'},{key:'carbon',label:'Carbon'},{key:'ppf-clear',label:'PPF Clear'},{key:'ppf-matte',label:'PPF Matte'} ];
    window.BRANDS = ['Avery Dennison','Hexis','STEK'];
    window.TIER_LABEL = { standard:{name:'Standard',note:''}, premium:{name:'Premium',note:''}, specialist:{name:'Specialist',note:''} };
    window.WRAP_CATALOGUE = C; })();
  (TIER_LABEL kept because app internals reference it; notes blanked since customer-facing tier copy is removed in Plan 03.)
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write build-catalogue.mjs (mapping + blob upload + write)</name>
  <read_first>
    - .planning/phases/05-integration-catalogue/05-RESEARCH.md (Pattern 4 build script structure, finish/tier/brand mapping tables, ppf-colour split verification)
    - scripts/backfill-clients.mjs (project ESM script conventions)
  </read_first>
  <files>scripts/build-catalogue.mjs</files>
  <action>
    Write an ESM script that:
    1. Reads source JSON from a path that defaults to `/Users/kieranredpath/Downloads/Wrap colours/Extract/wrap-colours.json` but is overridable via `process.argv[2]` or `SOURCE_JSON` env (do NOT silently hardcode without a fallback override).
    2. Walks `<source-dir>/swatches/stek/{colour-shift,gloss,matte}/` (and any other subdirs that exist) recursively, building `pngMap` = { entryId(stem) → absolutePath }. Expect 18 files.
    3. Defines `mapFinish(rawFinish, id)`, `mapBrand(raw)`, `inferTier(brand, rawFinish)`, `proTipFor(protoFinish, notes)`, and `specsFor(protoFinish)` per the mapping tables in <facts>.
    4. Uploads each PNG in `pngMap` to Vercel Blob SEQUENTIALLY via `put('wrap-studio/swatches/<id>.png', readFileSync(path), { access:'public', contentType:'image/png', token: process.env.BLOB_READ_WRITE_TOKEN })`, with a 100ms delay between uploads. Collect `{ id → blob.url }` into `swatchUrls`.
    5. Builds the 375-entry array: for each source entry produce { id, brand: mapBrand(brand), series, name, code, finish: mapFinish(finish,id), hex, hex2:null, tier: inferTier(brand,finish), thickness, conform, warranty (from specsFor), proTip: proTipFor(...), swatchUrl: swatchUrls[id] ?? null, hexConfidence: entry.hex_confidence ?? null }.
    6. Writes `public/wrap-studio/catalogue.js` as the IIFE shown in <facts> (use JSON.stringify for the C array, then wrap). FINISHES/BRANDS/TIER_LABEL exactly as specified.
    7. Logs: total entries written, count of swatchUrls populated, count of low-confidence entries.
    Use `import { put } from '@vercel/blob'` and `import { readFileSync, readdirSync, writeFileSync, statSync } from 'fs'`.
  </action>
  <verify>
    <automated>node --check scripts/build-catalogue.mjs && grep -q "from '@vercel/blob'" scripts/build-catalogue.mjs && grep -q "window.WRAP_CATALOGUE" scripts/build-catalogue.mjs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/build-catalogue.mjs` exits 0
    - script imports `put` from '@vercel/blob'
    - script references both 'metallic' and 'carbon' as preserved finish keys
    - script reads `process.env.BLOB_READ_WRITE_TOKEN` and passes it to put()
    - FINISHES array literal in the generated output has 9 entries including metallic and carbon
  </acceptance_criteria>
  <done>build-catalogue.mjs parses and contains the complete mapping + upload + write pipeline.</done>
</task>

<task type="auto">
  <name>Task 2: Write validate-catalogue.mjs, run build, run validate</name>
  <read_first>
    - .planning/phases/05-integration-catalogue/05-VALIDATION.md (per-task verification map: 375 entries, 18 blob URLs)
  </read_first>
  <files>scripts/validate-catalogue.mjs</files>
  <action>
    1. Write `scripts/validate-catalogue.mjs` (ESM): load `public/wrap-studio/catalogue.js` by reading the file and evaluating it in a minimal `global.window = {}` sandbox (e.g. `const window={}; eval(fs.readFileSync(...))` or `new Function('window', src)(window)`), then assert:
       - `window.WRAP_CATALOGUE.length === 375` (else process.exit(1))
       - every entry has non-empty `code`, `series`, `finish`, and a `brand` in ['Avery Dennison','Hexis','STEK']
       - count of entries with non-null `swatchUrl` === 18
       - `window.FINISHES.length === 9` and includes keys 'metallic' and 'carbon'
       - `window.BRANDS` deep-equals ['Avery Dennison','Hexis','STEK']
       Print a PASS summary; exit 1 on any failure.
    2. Run the build: `node --env-file=.env.local scripts/build-catalogue.mjs`. This overwrites public/wrap-studio/catalogue.js and performs the 18 blob uploads.
    3. Run the validator: `node scripts/validate-catalogue.mjs` — must print PASS and exit 0.
  </action>
  <verify>
    <automated>node --check scripts/validate-catalogue.mjs && node --env-file=.env.local scripts/build-catalogue.mjs && node scripts/validate-catalogue.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-catalogue.mjs` exits 0 and prints PASS
    - `grep -c '"code"' public/wrap-studio/catalogue.js` is ≥ 375
    - `grep -c 'blob.vercel-storage.com' public/wrap-studio/catalogue.js` === 18
    - `grep -o "key: 'metallic'\|key:'metallic'\|\"metallic\"" public/wrap-studio/catalogue.js | head -1` is non-empty (metallic chip present)
    - generated catalogue.js contains `window.WRAP_CATALOGUE`
  </acceptance_criteria>
  <done>Validator passes: 375 entries, 18 blob URLs, 9 finish chips including metallic+carbon, correct brand display names.</done>
</task>

</tasks>

<verification>
- `node scripts/validate-catalogue.mjs` → PASS, exit 0.
- `grep -c 'blob.vercel-storage.com' public/wrap-studio/catalogue.js` → 18.
- Manual (dev server): open /mc-site/wrap-studio, browser console `window.WRAP_CATALOGUE.length` → 375; STEK DYNO swatches show real images, Avery/Hexis show hex backgrounds (no broken image icons).
</verification>

<success_criteria>
- CAT-01: all 375 films browsable.
- CAT-05: each shows real code, series, finish.
- CAT-06: 18 STEK swatches load from blob; 357 fall back to hex.
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-catalogue/05-02-SUMMARY.md`
</output>
