---
phase: 05-integration-catalogue
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - public/wrap-studio/catalogue-panel.jsx
  - public/wrap-studio/studio.css
  - public/wrap-studio/app.jsx
  - app/mc-site/wrapping/page.jsx
autonomous: true
requirements: [CAT-02, CAT-03, CAT-04, CAT-05, INT-01]
must_haves:
  truths:
    - "Swatch chips render the real Vercel Blob image when swatchUrl is set, hex fallback otherwise"
    - "Low-confidence entries show a ~ approximate indicator on the chip"
    - "No tier or pricing text appears anywhere customer-facing (quote footer or quote modal)"
    - "Metallic and Carbon finish chips filter correctly"
    - "Wrapping page has a 'Wrap Studio →' CTA linking to /mc-site/wrap-studio"
  artifacts:
    - path: "public/wrap-studio/catalogue-panel.jsx"
      provides: "Swatch image rendering, ~ indicator, tier-free quote footer"
      contains: "swatchUrl"
    - path: "public/wrap-studio/app.jsx"
      provides: "Quote modal with tier removed"
    - path: "app/mc-site/wrapping/page.jsx"
      provides: "CTA link to wrap studio"
      contains: "/mc-site/wrap-studio"
  key_links:
    - from: "public/wrap-studio/catalogue-panel.jsx"
      to: "sw.swatchUrl blob image"
      via: "img tag as first child of .sw-chip"
      pattern: "swatchUrl"
    - from: "app/mc-site/wrapping/page.jsx"
      to: "/mc-site/wrap-studio"
      via: "anchor href"
      pattern: "/mc-site/wrap-studio"
---

<objective>
Update `catalogue-panel.jsx` to render Blob swatch images with hex fallback and a low-confidence `~` indicator, strip all tier/pricing UI from the quote footer (catalogue-panel.jsx) and quote modal (app.jsx), and add the "Wrap Studio →" CTA to the wrapping page. After this plan the catalogue is fully production-correct: real imagery, no pricing language, all 9 finish filters working, and discoverable from the wrapping page.

Purpose: CAT-02/03/04 filtering, CAT-05 imagery detail, INT-01 discoverability, and the locked "no pricing shown" decision.
Output: modified catalogue-panel.jsx, studio.css, app.jsx, wrapping/page.jsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-integration-catalogue/05-UI-SPEC.md

<facts>
- catalogue-panel.jsx uses React.createElement aliased as `h`. Icons via `I` (window.Icon). FINISHES/BRANDS read from window globals.
- Brand filter (CAT-02), finish filter (CAT-03), search by name/code (CAT-04) are ALREADY implemented in the panel's filter logic against sw.brand / sw.finish / sw.name / sw.code. They work automatically once catalogue.js (Plan 02) supplies correct brand display names + finish keys + the 9-chip FINISHES array. No filter-logic changes needed — only verify.

SWATCH IMAGE RENDERING (CAT-05/06): In the `Swatch` component, `.sw-chip` is currently:
  h('div', { className: 'sw-chip', style: { background: chipBg(sw) } }, chipInner(sw), <sw-tier span>, <sw-fav span>)
Change so that when `sw.swatchUrl` is truthy, an img is the FIRST child of .sw-chip:
  h('img', { src: sw.swatchUrl, alt: sw.name, style: { width:'100%', height:'100%', objectFit:'cover', display:'block' } })
Keep `background: chipBg(sw)` on .sw-chip (hex shows while image loads / when null). When swatchUrl is null, render NO img (hex background only — no broken icon).

LOW-CONFIDENCE INDICATOR (UI-SPEC "Low hex_confidence treatment"): when `sw.hexConfidence === 'low'`, add inside .sw-chip (same layer as sw-tier):
  h('span', { className: 'sw-approx', title: 'Colour is approximate — ' + sw.finish + ' films shift with angle and light' }, '~')

TIER REMOVAL — quote footer (catalogue-panel.jsx ~lines 152-156): DELETE the `quote-tier` block (h('div',{className:'quote-tier'},...) containing `.tt` 'Price tier' and `.tv` tierInfo.name) AND the `quote-note` line (tierInfo.note + '. Real number comes back fast...'). KEEP the `.quote-sel` colour-name block and the primary "Get a quote for this wrap" button. Remove the now-dead `topTier`/`tierInfo` locals only if removal does not break other references; otherwise leave them declared.

TIER REMOVAL — quote modal (app.jsx ~lines 229-239): in the `.q-summary` column DELETE the `.qs-tier` badge span (~line 233: h('span',{className:'qs-tier'...}, tier.name + ' tier')) and change the `.q-note` text (~line 239) from the tier-note string to exactly:
  "We'll confirm availability and come back with a fixed price — usually same day."
KEEP the colour-selection `.q-line` items and the entire form column.

CTA on wrapping page: app/mc-site/wrapping/page.jsx is a Next.js server component (JSX). The `svc-intro` section ends ~line 85 after an intro `<p>`. Insert a styled CTA anchor as a sibling after that paragraph, using existing class `link-arrow`:
  <a className="link-arrow" href="/mc-site/wrap-studio">Try the Wrap Studio →</a>
Do not disturb existing markup.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swatch image rendering + low-confidence ~ indicator</name>
  <read_first>
    - public/wrap-studio/catalogue-panel.jsx (Swatch component ~lines 23-36, chipBg ~line 18)
    - public/wrap-studio/studio.css (search for `.sw-tier` and `.sw-chip` and `.sw-approx`)
    - .planning/phases/05-integration-catalogue/05-UI-SPEC.md (Swatch images from Vercel Blob + Low hex_confidence treatment)
  </read_first>
  <files>
    public/wrap-studio/catalogue-panel.jsx
    public/wrap-studio/studio.css
  </files>
  <action>
    1. In the `Swatch` component, render `h('img', {...})` (style per <facts>) as the FIRST child of `.sw-chip` when `sw.swatchUrl` is truthy; keep `background: chipBg(sw)` on .sw-chip for fallback. When swatchUrl is falsy, render no img.
    2. Add the `~` indicator span (className 'sw-approx', title per <facts>) inside .sw-chip when `sw.hexConfidence === 'low'`.
    3. Check studio.css for `.sw-approx`. If missing, add the pill style per UI-SPEC: position absolute, bottom-left of `.sw-chip`, font-family var(--f-mono), font-size 8px, letter-spacing .1em, background rgba(10,10,10,.6), border 1px solid var(--line-2), color rgba(255,255,255,.55), padding 2px 5px, border-radius to match `.sw-tier`. Verify `.sw-chip` is position:relative (sw-tier/sw-fav are absolutely positioned, so it should be) — do not change its positioning if already set.
  </action>
  <verify>
    <automated>grep -q "swatchUrl" public/wrap-studio/catalogue-panel.jsx && grep -q "sw-approx" public/wrap-studio/catalogue-panel.jsx && grep -q "hexConfidence" public/wrap-studio/catalogue-panel.jsx && grep -q "sw-approx" public/wrap-studio/studio.css && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - catalogue-panel.jsx Swatch renders an img with src=sw.swatchUrl guarded by truthiness
    - `grep -c "sw-approx" public/wrap-studio/catalogue-panel.jsx` ≥ 1
    - `grep -c "sw-approx" public/wrap-studio/studio.css` ≥ 1
    - hex fallback (chipBg) still applied as .sw-chip background
  </acceptance_criteria>
  <done>Swatch chips show blob images when present, hex otherwise; low-confidence entries show ~ with tooltip.</done>
</task>

<task type="auto">
  <name>Task 2: Remove all tier/pricing UI from quote footer and quote modal</name>
  <read_first>
    - public/wrap-studio/catalogue-panel.jsx (quote footer ~lines 145-157)
    - public/wrap-studio/app.jsx (quote modal q-summary ~lines 229-240)
    - .planning/phases/05-integration-catalogue/05-UI-SPEC.md (Copywriting Contract "Removed copy" + Quote Modal Redesign)
  </read_first>
  <files>
    public/wrap-studio/catalogue-panel.jsx
    public/wrap-studio/app.jsx
  </files>
  <action>
    1. In catalogue-panel.jsx quote footer: delete the `quote-tier` div block (Price tier + tierInfo.name) and the `quote-note` line entirely. Keep `.quote-sel` and the "Get a quote for this wrap" button. Clean up `topTier`/`tierInfo` if they become unused without breaking other refs.
    2. In app.jsx quote modal `.q-summary`: delete the `.qs-tier` badge span, and replace the `.q-note` text with exactly: "We'll confirm availability and come back with a fixed price — usually same day."
    3. Confirm no customer-facing "Standard"/"Premium"/"Specialist"/"Price tier"/"tier" string remains in the rendered quote footer or modal (window.TIER_LABEL may still exist for internal use, but must not be rendered to the customer).
  </action>
  <verify>
    <automated>! grep -q "Price tier" public/wrap-studio/catalogue-panel.jsx && ! grep -q "qs-tier" public/wrap-studio/app.jsx && grep -q "fixed price — usually same day" public/wrap-studio/app.jsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Price tier" public/wrap-studio/catalogue-panel.jsx` === 0
    - `grep -c "quote-tier" public/wrap-studio/catalogue-panel.jsx` === 0
    - `grep -c "qs-tier" public/wrap-studio/app.jsx` === 0
    - app.jsx contains the new q-note string
  </acceptance_criteria>
  <done>No tier/pricing labels visible in quote footer or modal; q-note reads the fixed-price line.</done>
</task>

<task type="auto">
  <name>Task 3: Add Wrap Studio CTA to wrapping page</name>
  <read_first>
    - app/mc-site/wrapping/page.jsx (svc-intro section ~lines 80-86)
  </read_first>
  <files>app/mc-site/wrapping/page.jsx</files>
  <action>
    Insert a CTA anchor inside the `svc-intro` section, as a sibling immediately after the intro `<p>...</p>`:
      <a className="link-arrow" href="/mc-site/wrap-studio">Try the Wrap Studio →</a>
    Preserve all surrounding markup and the existing container/section structure.
  </action>
  <verify>
    <automated>grep -q "/mc-site/wrap-studio" app/mc-site/wrapping/page.jsx && grep -q "Wrap Studio" app/mc-site/wrapping/page.jsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "/mc-site/wrap-studio" app/mc-site/wrapping/page.jsx` ≥ 1
    - anchor text contains "Wrap Studio"
    - file still parses as valid JSX (no unclosed tags) — `npm run build` or `next lint` on this file succeeds if run
  </acceptance_criteria>
  <done>Wrapping page shows a working CTA link to /mc-site/wrap-studio.</done>
</task>

</tasks>

<verification>
- All three task automated checks pass.
- Manual (dev server): /mc-site/wrap-studio — STEK swatches show images, Avery/Hexis show hex; chrome/shift chips show `~`; brand tabs (All/Avery Dennison/Hexis/STEK) filter; finish chips include Metallic and Carbon and filter; search "Carmine" and "SW900" both narrow results; quote footer and modal show NO tier/pricing.
- Manual: /mc-site/wrapping shows "Try the Wrap Studio →" linking to the studio.
</verification>

<success_criteria>
- CAT-02: brand filter works with display names.
- CAT-03: all 9 finish chips (incl. Metallic, Carbon) filter correctly.
- CAT-04: name + code search narrows results.
- CAT-05: swatch detail shows real imagery + approximate indicator.
- INT-01: studio discoverable via wrapping page CTA.
- No pricing/tier language anywhere customer-facing.
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-catalogue/05-03-SUMMARY.md`
</output>
