---
phase: 05-integration-catalogue
plan: 04
type: execute
wave: 4
depends_on: ["05-01", "05-02", "05-03"]
files_modified: []
autonomous: false
requirements: [INT-01, INT-02, INT-03, CAT-01, CAT-02, CAT-03, CAT-04, CAT-05, CAT-06]
must_haves:
  truths:
    - "Studio loads at /mc-site/wrap-studio within ~3s on phone and desktop without login"
    - "All 375 films are browsable with real code/series/finish"
    - "Brand, finish, and search filters all work"
    - "Studio is usable on a phone viewport with no horizontal scroll"
  artifacts:
    - path: ".planning/phases/05-integration-catalogue/05-04-SUMMARY.md"
      provides: "UAT sign-off record"
  key_links:
    - from: "all prior plans"
      to: "phase success criteria"
      via: "human verification of the 9 requirements"
      pattern: "approved"
---

<objective>
User Acceptance Test for Phase 5. Claude runs all automated smoke checks first, then the user verifies the 5 phase success criteria and 9 requirements in a real browser (desktop + phone viewport). This is the gate before marking the phase complete.

Purpose: Confirm INT-01/02/03 and CAT-01..06 are genuinely TRUE from the customer's perspective.
Output: 05-04-SUMMARY.md recording the UAT result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-integration-catalogue/05-VALIDATION.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated smoke checks</name>
  <read_first>
    - .planning/phases/05-integration-catalogue/05-VALIDATION.md (per-task verification map)
  </read_first>
  <files></files>
  <action>
    With the dev server running (start `npm run dev` if needed), run and report results for:
    1. `curl -s http://localhost:3000/mc-site/wrap-studio -o /dev/null -w "%{http_code}\n"` → expect 200
    2. `curl -s http://localhost:3000/mc-site/wrap-studio | grep -c "wrap-studio/app.jsx"` → expect ≥1
    3. `node scripts/validate-catalogue.mjs` → expect PASS exit 0
    4. `grep -c 'blob.vercel-storage.com' public/wrap-studio/catalogue.js` → expect 18
    5. `grep -c "Price tier" public/wrap-studio/catalogue-panel.jsx` → expect 0
    6. `grep -c "/mc-site/wrap-studio" app/mc-site/wrapping/page.jsx` → expect ≥1
    Summarise pass/fail for each. If any fail, STOP and report which — do not proceed to the human checkpoint.
  </action>
  <verify>
    <automated>node scripts/validate-catalogue.mjs && echo "catalogue OK" && grep -c 'blob.vercel-storage.com' public/wrap-studio/catalogue.js | grep -qE '^1[0-9]$' && echo "blob URLs OK"</automated>
    <manual>With dev server running (npm run dev): curl -s http://localhost:3000/mc-site/wrap-studio -o /dev/null -w "%{http_code}\n" → expect 200</manual>
  </verify>
  <acceptance_criteria>
    - All 6 automated checks pass with expected values
    - validate-catalogue.mjs exits 0
  </acceptance_criteria>
  <done>All automated smoke checks green; ready for human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT — verify all 9 requirements in browser</name>
  <action>Present the verification steps below to the user and wait for approval. Do not mark the phase complete until the user types "approved".</action>
  <what-built>
    Phase 5 complete: wrap studio live at /mc-site/wrap-studio with all 375 real wrap films, brand/finish/search filtering, real STEK swatch imagery (hex fallback elsewhere), no pricing language, and a CTA from the wrapping page.
  </what-built>
  <how-to-verify>
    DESKTOP (browser at http://localhost:3000):
    1. Visit /mc-site/wrap-studio — loads full-screen, no mc-site nav bar, no demo car, stage shows "DROP YOUR CAR PHOTO". Feels fast (<3s). [INT-01, INT-02]
    2. Open DevTools console: type `window.WRAP_CATALOGUE.length` → 375. [CAT-01]
    3. Each swatch shows a product code + name; STEK DYNO swatches show real images, Avery/Hexis show solid hex colours (no broken-image icons). [CAT-05, CAT-06]
    4. Brand tabs: click All / Avery Dennison / Hexis / STEK — list narrows correctly; counter updates. [CAT-02]
    5. Finish chips: confirm Gloss, Satin, Matte, Metallic, Chrome, Colour-shift, Carbon, PPF Clear, PPF Matte all present; click Metallic and Carbon — results appear. [CAT-03]
    6. Search "Carmine" → narrows; search "SW900" → narrows by code. [CAT-04]
    7. Filter to Chrome / Colour-shift — swatch chips show a small `~` indicator. [CAT-05]
    8. Select a colour, click "Get a quote for this wrap" — quote footer and modal show NO "tier"/"Standard/Premium/Specialist"/price text anywhere.
    9. Visit /mc-site/wrapping — find "Try the Wrap Studio →" CTA; click it → lands on the studio. [INT-01]

    PHONE (DevTools device toolbar, 390px width, e.g. iPhone 12):
    10. /mc-site/wrap-studio is usable — panel moves to bottom, stage above; NO horizontal scrolling; swatches tappable; quote modal stacks single-column. [INT-03]
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found (which requirement, what's wrong).</resume-signal>
</task>

</tasks>

<verification>
- All automated smoke checks pass.
- Human confirms all 9 requirements + 5 success criteria observable in browser (desktop + phone).
</verification>

<success_criteria>
- INT-01, INT-02, INT-03 and CAT-01..06 all verified TRUE.
- User types "approved".
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-catalogue/05-04-SUMMARY.md` recording the UAT outcome and any follow-ups.
</output>
