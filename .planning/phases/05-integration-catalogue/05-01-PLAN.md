---
phase: 05-integration-catalogue
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/mc-site/wrap-studio/route.js
  - public/wrap-studio/app.jsx
  - public/wrap-studio/catalogue-panel.jsx
  - public/wrap-studio/icons.jsx
  - public/wrap-studio/stage.jsx
  - public/wrap-studio/studio.css
  - public/wrap-studio/tweaks-panel.jsx
  - public/wrap-studio/catalogue.js
autonomous: true
requirements: [INT-01, INT-02]
must_haves:
  truths:
    - "Customer can visit /mc-site/wrap-studio without logging in and the studio loads"
    - "Studio renders full-screen without the mc-site header/nav"
    - "No demo car appears — the stage shows the 'Drop your car photo' empty state"
    - "JS/CSS load from /wrap-studio/ static paths"
  artifacts:
    - path: "app/mc-site/wrap-studio/route.js"
      provides: "GET handler returning standalone HTML shell that loads studio assets"
      contains: "export async function GET"
    - path: "public/wrap-studio/app.jsx"
      provides: "Studio root component with DEMO_CAR_SRC nulled"
      contains: "DEMO_CAR_SRC = null"
    - path: "public/wrap-studio/studio.css"
      provides: "Studio styles copied from prototype"
    - path: "public/wrap-studio/catalogue.js"
      provides: "Placeholder catalogue (replaced by build script in Plan 02)"
  key_links:
    - from: "app/mc-site/wrap-studio/route.js"
      to: "public/wrap-studio/app.jsx"
      via: "script src tag"
      pattern: "wrap-studio/app.jsx"
    - from: "public/wrap-studio/app.jsx"
      to: "stage empty state"
      via: "DEMO_CAR_SRC null falls through to drop state"
      pattern: "DEMO_CAR_SRC = null"
---

<objective>
Scaffold the wrap studio route and copy the 7 design-system prototype files into `public/wrap-studio/`, patching `app.jsx` to null the demo car. After this plan the studio loads full-screen at `/mc-site/wrap-studio` with the real prototype UI, the demo catalogue, and no demo car — ready for the real catalogue (Plan 02).

Purpose: Establish INT-01 (public route) and INT-02 (fast static asset serving) and the integration substrate every later plan builds on.
Output: `app/mc-site/wrap-studio/route.js` + 7 files in `public/wrap-studio/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-integration-catalogue/05-CONTEXT.md
@.planning/phases/05-integration-catalogue/05-RESEARCH.md

# Prototype source files (copy these verbatim except app.jsx patch):
# /tmp/mc-wrap-studio/wrap-studio/{app.jsx,catalogue-panel.jsx,catalogue.js,icons.jsx,stage.jsx,studio.css,tweaks-panel.jsx}

<facts>
- Project is ESM ("type": "module" in package.json) — confirmed.
- The prototype is React UMD (React.createElement aliased as `h`), NOT a Next.js component tree. It must be served as a raw HTML document.
- DECISION OVERRIDE: Use a Route Handler (`route.js` with GET export), NOT page.jsx. This bypasses ALL mc-site layout nesting (the only reliable full-screen approach — verified in RESEARCH.md Pattern 3 + Pitfall 2). CONTEXT.md says page.jsx but the locked key-decision is route.js.
- app.jsx line 31 currently reads: `const DEMO_CAR_SRC = 'wrap-studio/_DEMO-car-REMOVE-BEFORE-PROD.png';` — must become `const DEMO_CAR_SRC = null;`
- The demo car PNG `_DEMO-car-REMOVE-BEFORE-PROD.png` is NOT copied.
- catalogue.js is copied as-is in THIS plan (demo data) and REPLACED by Plan 02's build script. Copying it here keeps the studio renderable for INT-01/INT-02 verification.
- TweaksPanel is self-suppressing (renders only on a `__activate_edit_mode` postMessage that never arrives in production) — copy it verbatim, NO production check needed. Per UI-SPEC: do NOT add env checks or remove it.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy prototype files into public/wrap-studio/ and null the demo car</name>
  <read_first>
    - /tmp/mc-wrap-studio/wrap-studio/app.jsx (whole file — find DEMO_CAR_SRC at line ~31)
  </read_first>
  <files>
    public/wrap-studio/app.jsx
    public/wrap-studio/catalogue-panel.jsx
    public/wrap-studio/catalogue.js
    public/wrap-studio/icons.jsx
    public/wrap-studio/stage.jsx
    public/wrap-studio/studio.css
    public/wrap-studio/tweaks-panel.jsx
  </files>
  <action>
    1. Create directory `public/wrap-studio/`.
    2. Copy these 7 files verbatim from `/tmp/mc-wrap-studio/wrap-studio/` to `public/wrap-studio/`:
       app.jsx, catalogue-panel.jsx, catalogue.js, icons.jsx, stage.jsx, studio.css, tweaks-panel.jsx
    3. Do NOT copy `_DEMO-car-REMOVE-BEFORE-PROD.png`.
    4. In `public/wrap-studio/app.jsx` ONLY, change the line:
         `const DEMO_CAR_SRC = 'wrap-studio/_DEMO-car-REMOVE-BEFORE-PROD.png';`
       to:
         `const DEMO_CAR_SRC = null;`
       Leave all other lines (the comment block above it, `isDemo`, `carUrl` init) unchanged — they handle null correctly.
    5. Do NOT modify tweaks-panel.jsx (self-suppressing by design — UI-SPEC).
  </action>
  <verify>
    <automated>test -f public/wrap-studio/app.jsx && test -f public/wrap-studio/studio.css && test -f public/wrap-studio/catalogue.js && test ! -f public/wrap-studio/_DEMO-car-REMOVE-BEFORE-PROD.png && grep -q 'DEMO_CAR_SRC = null' public/wrap-studio/app.jsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `ls public/wrap-studio/ | wc -l` returns 7
    - `grep -c "DEMO_CAR_SRC = null" public/wrap-studio/app.jsx` returns 1
    - `grep -c "_DEMO-car-REMOVE-BEFORE-PROD" public/wrap-studio/app.jsx` returns 0 (the var no longer references the file)
    - demo PNG is absent from public/wrap-studio/
  </acceptance_criteria>
  <done>All 7 prototype files present in public/wrap-studio/, demo car PNG absent, DEMO_CAR_SRC nulled.</done>
</task>

<task type="auto">
  <name>Task 2: Create the route handler serving the standalone HTML shell</name>
  <read_first>
    - .planning/phases/05-integration-catalogue/05-RESEARCH.md (Pattern 3 / "Route handler pattern" code block, lines ~504-531)
    - app/mc-site/layout.jsx (confirm it wraps children — this is what route.js bypasses)
  </read_first>
  <files>app/mc-site/wrap-studio/route.js</files>
  <action>
    Create `app/mc-site/wrap-studio/route.js` exporting an async `GET()` that returns a `Response` with a full HTML document (Content-Type: text/html; charset=utf-8). Use exactly this script load ORDER (critical for Babel — see RESEARCH Pitfall 1): React UMD, ReactDOM UMD, Babel standalone, catalogue.js (plain script, NO type="text/babel"), then icons.jsx, tweaks-panel.jsx, stage.jsx, catalogue-panel.jsx, app.jsx (each as `<script type="text/babel" src="...">`).

    HTML head must include:
      - `<meta charset="UTF-8">`
      - `<meta name="viewport" content="width=device-width, initial-scale=1">`
      - `<meta name="robots" content="noindex">`
      - `<title>Wrap Studio — Matthews / Clark</title>`
      - `<link rel="stylesheet" href="/wrap-studio/studio.css">`
    Body: `<div id="app"></div>` followed by the scripts in the order above.

    CDN sources (pin major versions):
      - https://unpkg.com/react@18/umd/react.production.min.js
      - https://unpkg.com/react-dom@18/umd/react-dom.production.min.js
      - https://unpkg.com/@babel/standalone/babel.min.js

    All studio asset srcs are root-relative `/wrap-studio/<file>`.
    Do NOT add `export const metadata` (route handlers don't support it) — the noindex meta covers SEO.
  </action>
  <verify>
    <automated>node --check app/mc-site/wrap-studio/route.js && grep -q "export async function GET" app/mc-site/wrap-studio/route.js && grep -q '/wrap-studio/app.jsx' app/mc-site/wrap-studio/route.js && grep -q 'type="text/babel"' app/mc-site/wrap-studio/route.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/mc-site/wrap-studio/route.js` exits 0
    - route.js contains `export async function GET`
    - catalogue.js script tag has NO `type="text/babel"` (loads as plain global script)
    - app.jsx is the LAST script tag and HAS `type="text/babel"`
    - HTML contains `<meta name="robots" content="noindex">`
    - script order in file: react → react-dom → babel → catalogue.js → icons.jsx → tweaks-panel.jsx → stage.jsx → catalogue-panel.jsx → app.jsx
  </acceptance_criteria>
  <done>Route handler exists, parses, returns the HTML shell loading all studio assets in correct Babel-safe order.</done>
</task>

</tasks>

<verification>
- `node --check app/mc-site/wrap-studio/route.js` passes.
- With `npm run dev` running: `curl -s http://localhost:3000/mc-site/wrap-studio -o /dev/null -w "%{http_code}"` returns 200.
- `curl -s http://localhost:3000/mc-site/wrap-studio | grep -c "wrap-studio/app.jsx"` returns ≥1.
- Manual: open http://localhost:3000/mc-site/wrap-studio — studio renders full-screen, NO mc-site nav bar, stage shows "DROP YOUR CAR PHOTO" empty state (no demo car), demo catalogue swatches visible in panel.
</verification>

<success_criteria>
- INT-01: /mc-site/wrap-studio returns 200 with no auth.
- INT-02: All JS/CSS served from /wrap-studio/ static paths (Vercel CDN edge-cached).
- No demo car; full-screen layout with no mc-site chrome.
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-catalogue/05-01-SUMMARY.md`
</output>
