---
phase: 09-webhook-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - db/migrations/001-schema.sql
  - scripts/run-migration.mjs
autonomous: true
requirements: [FOUND-07]
requirements_addressed: [FOUND-07]
user_setup:
  - service: neon
    why: "Postgres storage for WhatsApp conversations (provisioned via Vercel Marketplace)"
    env_vars:
      - name: DATABASE_URL
        source: "Vercel Dashboard -> Storage -> Neon -> pooled connection string (-pooler.neon.tech)"
      - name: DATABASE_URL_UNPOOLED
        source: "Vercel Dashboard -> Storage -> Neon -> direct connection string (used for migration only)"

must_haves:
  truths:
    - "All 7 Neon tables exist after running the migration"
    - "whatsapp_messages.wamid has a UNIQUE constraint (dedup safety net for Meta at-least-once delivery)"
    - "@neondatabase/serverless is installed and importable"
  artifacts:
    - path: "db/migrations/001-schema.sql"
      provides: "DDL for all 7 tables per FOUND-07"
      contains: "CREATE TABLE IF NOT EXISTS whatsapp_messages"
    - path: "scripts/run-migration.mjs"
      provides: "Node runner that executes the migration against DATABASE_URL_UNPOOLED"
      contains: "DATABASE_URL_UNPOOLED"
    - path: "package.json"
      provides: "@neondatabase/serverless dependency"
      contains: "@neondatabase/serverless"
  key_links:
    - from: "scripts/run-migration.mjs"
      to: "db/migrations/001-schema.sql"
      via: "readFileSync + sql query execution"
      pattern: "001-schema\\.sql"
---

<objective>
Install the only new dependency for Phase 09 (`@neondatabase/serverless`), author the single migration file defining all 7 Neon tables (FOUND-07), and provide a runner script that applies the schema against the direct (unpooled) connection.

Purpose: Everything downstream (the store layer, the webhook) writes into these tables. The schema and driver must exist before any read/write code can be authored. This is the Wave 1 foundation.
Output: `@neondatabase/serverless` in package.json, `db/migrations/001-schema.sql`, `scripts/run-migration.mjs`, and a live schema in Neon.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-webhook-foundation/09-CONTEXT.md
@.planning/phases/09-webhook-foundation/09-RESEARCH.md

The migration SQL is fully specified in 09-RESEARCH.md "Neon Schema — All 7 Tables (FOUND-07)" (lines 422-520). Use those exact table and column definitions — they are the locked names per CONTEXT.md.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @neondatabase/serverless</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (confirm current deps; type is "module"; no neon package present yet)
    - .planning/phases/09-webhook-foundation/09-RESEARCH.md (Standard Stack section — version is ^1.1.0, confirmed latest)
  </read_first>
  <action>
    Run `npm install @neondatabase/serverless@^1.1.0` in the repo root.
    This adds `"@neondatabase/serverless": "^1.1.0"` to the `dependencies` block of package.json and updates package-lock.json.
    Do NOT add `serverExternalPackages` to next.config — the package is pure JS with no WASM; only add it later if `next build` fails with a neon bundling error (per RESEARCH Open Question 5).
  </action>
  <verify>
    <automated>node -e "require('@neondatabase/serverless'); console.log('neon ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep '@neondatabase/serverless' package.json` returns a line under dependencies
    - `node -e "require('@neondatabase/serverless')"` exits 0
    - `ls node_modules/@neondatabase/serverless` lists package files
  </acceptance_criteria>
  <done>@neondatabase/serverless is listed in package.json dependencies and importable from Node.</done>
</task>

<task type="auto">
  <name>Task 2: Author db/migrations/001-schema.sql with all 7 tables</name>
  <files>db/migrations/001-schema.sql</files>
  <read_first>
    - .planning/phases/09-webhook-foundation/09-RESEARCH.md (lines 422-520 — exact SQL for all 7 tables)
    - .planning/phases/09-webhook-foundation/09-CONTEXT.md (locked table names; nullable crm_lead_id; denormalised last_message_at/unread_count)
  </read_first>
  <action>
    Create `db/migrations/001-schema.sql` containing the DDL for all 7 tables EXACTLY as defined in 09-RESEARCH.md lines 422-520. The seven tables, in dependency order (threads before messages because messages FK threads):
      1. team_numbers (id UUID PK gen_random_uuid, wa_id TEXT UNIQUE NOT NULL, phone_number_id TEXT UNIQUE NOT NULL, display_name TEXT NOT NULL, active BOOLEAN DEFAULT true, created_at TIMESTAMPTZ DEFAULT now())
      2. whatsapp_threads (id UUID PK, contact_wa_id TEXT NOT NULL, team_wa_id TEXT NOT NULL, crm_lead_id TEXT NULLABLE, crm_client_id TEXT NULLABLE, contact_name TEXT, last_message_at TIMESTAMPTZ, last_message_preview TEXT, unread_count INT DEFAULT 0, created_at TIMESTAMPTZ DEFAULT now(), UNIQUE(contact_wa_id, team_wa_id))
      3. whatsapp_messages (id UUID PK, wamid TEXT UNIQUE NOT NULL, thread_id UUID NOT NULL REFERENCES whatsapp_threads(id), direction TEXT NOT NULL CHECK (direction IN ('inbound','outbound')), from_wa_id TEXT NOT NULL, to_wa_id TEXT NOT NULL, message_type TEXT NOT NULL, body TEXT, media_url TEXT, timestamp_ms BIGINT NOT NULL, created_at TIMESTAMPTZ DEFAULT now())
      4. push_subscriptions (id UUID PK, device_id TEXT UNIQUE NOT NULL, endpoint TEXT NOT NULL, p256dh TEXT NOT NULL, auth TEXT NOT NULL, user_agent TEXT, created_at TIMESTAMPTZ DEFAULT now())
      5. lead_intelligence (id UUID PK, thread_id UUID UNIQUE NOT NULL REFERENCES whatsapp_threads(id), warmth_score INT CHECK (warmth_score BETWEEN 1 AND 10), objections JSONB DEFAULT '[]', status_signal TEXT, follow_up_at TIMESTAMPTZ, last_analysed TIMESTAMPTZ, updated_at TIMESTAMPTZ DEFAULT now())
      6. aftercare_events (id UUID PK, crm_lead_id TEXT NOT NULL, event_type TEXT NOT NULL, scheduled_at TIMESTAMPTZ NOT NULL, sent_at TIMESTAMPTZ, status TEXT DEFAULT 'pending', created_at TIMESTAMPTZ DEFAULT now())
      7. broadcast_campaigns (id UUID PK, name TEXT NOT NULL, template_name TEXT NOT NULL, target_criteria JSONB NOT NULL, status TEXT DEFAULT 'draft', sent_count INT DEFAULT 0, created_at TIMESTAMPTZ DEFAULT now(), run_at TIMESTAMPTZ)
    Use `CREATE TABLE IF NOT EXISTS` for every table (idempotent re-runs).
    Include these indexes verbatim:
      CREATE INDEX ON whatsapp_threads (last_message_at DESC);
      CREATE INDEX ON whatsapp_threads (crm_lead_id) WHERE crm_lead_id IS NOT NULL;
      CREATE INDEX ON whatsapp_messages (thread_id, timestamp_ms DESC);
      CREATE INDEX ON whatsapp_messages (created_at DESC);
      CREATE INDEX ON aftercare_events (status, scheduled_at);
    Add a top-of-file comment block: "Run against DATABASE_URL_UNPOOLED (direct connection — DDL requires session-level access; Neon PgBouncer pooler rejects session DDL). Runtime queries use DATABASE_URL (pooler)."
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE IF NOT EXISTS" db/migrations/001-schema.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CREATE TABLE IF NOT EXISTS" db/migrations/001-schema.sql` returns exactly `7`
    - `grep "wamid           TEXT UNIQUE NOT NULL\|wamid.*TEXT UNIQUE NOT NULL" db/migrations/001-schema.sql` matches (dedup constraint present)
    - All 7 names present: `grep -E "team_numbers|whatsapp_threads|whatsapp_messages|push_subscriptions|lead_intelligence|aftercare_events|broadcast_campaigns" db/migrations/001-schema.sql | wc -l` >= 7
    - `grep "DATABASE_URL_UNPOOLED" db/migrations/001-schema.sql` matches (comment documents direct connection)
    - whatsapp_threads CREATE TABLE appears before whatsapp_messages CREATE TABLE in the file (messages FK threads)
  </acceptance_criteria>
  <done>db/migrations/001-schema.sql defines all 7 tables idempotently with the wamid UNIQUE constraint and required indexes, ordered so FKs resolve.</done>
</task>

<task type="auto">
  <name>Task 3: Author scripts/run-migration.mjs and apply the schema</name>
  <files>scripts/run-migration.mjs, package.json</files>
  <read_first>
    - package.json (type is "module" → use ESM import syntax, .mjs)
    - db/migrations/001-schema.sql (the file this script executes)
    - .planning/phases/09-webhook-foundation/09-RESEARCH.md (Pitfall 5 — DDL must use DATABASE_URL_UNPOOLED)
  </read_first>
  <action>
    Create `scripts/run-migration.mjs` as an ESM script that:
      - imports `{ neon }` from "@neondatabase/serverless" and `{ readFileSync }` from "node:fs"
      - reads `db/migrations/001-schema.sql` from disk (resolve path relative to the repo root)
      - requires `process.env.DATABASE_URL_UNPOOLED` — if missing, print "Set DATABASE_URL_UNPOOLED (direct connection) before running migrations" and `process.exit(1)`
      - creates `const sql = neon(process.env.DATABASE_URL_UNPOOLED)`
      - executes the migration. Because the neon HTTP driver runs one statement per call, split the file on `;` into statements, filter out blank/comment-only fragments, and run each via `await sql.query(statement)` in order (use `sql.query()` for raw string statements, NOT the tagged-template form). Wrap in try/catch; on error log the failing statement and exit 1.
      - on success print "Migration applied: 7 tables" and exit 0
    Add an npm script to package.json: `"migrate": "node scripts/run-migration.mjs"`.
    NOTE: This script is run manually by the developer once DATABASE_URL_UNPOOLED is set in their shell. The executor should NOT attempt to run it in CI/sandbox where the env var is absent — verification below only confirms the script's guard behaviour.
  </action>
  <verify>
    <automated>node scripts/run-migration.mjs; test $? -eq 1 && echo "guard ok (exits 1 without DATABASE_URL_UNPOOLED)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "DATABASE_URL_UNPOOLED" scripts/run-migration.mjs` matches
    - `grep "001-schema.sql" scripts/run-migration.mjs` matches
    - `grep '"migrate"' package.json` matches
    - Running `node scripts/run-migration.mjs` with no DATABASE_URL_UNPOOLED set exits with code 1 and prints the guard message (does NOT throw an unhandled neon connection error)
    - `node --check scripts/run-migration.mjs` exits 0 (valid ESM syntax)
  </acceptance_criteria>
  <done>scripts/run-migration.mjs exists, guards on the missing direct connection string, and applies the migration when DATABASE_URL_UNPOOLED is set. `npm run migrate` is wired.</done>
</task>

</tasks>

<verification>
- `node -e "require('@neondatabase/serverless')"` succeeds
- `grep -c "CREATE TABLE IF NOT EXISTS" db/migrations/001-schema.sql` === 7
- `node --check scripts/run-migration.mjs` passes
- After developer runs `DATABASE_URL_UNPOOLED=... npm run migrate`, all 7 tables exist in Neon (manual verification at execution time with real env)
</verification>

<success_criteria>
- FOUND-07 satisfied: all 7 tables defined in one idempotent migration with correct FK ordering and the wamid UNIQUE dedup constraint
- The neon serverless driver is installed and the runner is ready to apply the schema
</success_criteria>

<output>
After completion, create `.planning/phases/09-webhook-foundation/09-01-SUMMARY.md`
</output>
