---
phase: 10-web-push-notifications
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - public/sw.js
  - app/(crm)/admin/(protected)/kit/push-subscribe.jsx
  - app/(crm)/admin/(protected)/kit/app.jsx
  - app/(crm)/admin/(protected)/kit/screens-settings.jsx
autonomous: false
requirements: [NOTIF-01, NOTIF-02, NOTIF-03]

must_haves:
  truths:
    - "A team member running the CRM as an installed PWA sees a subscribe row in Settings (hidden in a normal browser tab)"
    - "Tapping subscribe requests permission, subscribes via pushManager, and POSTs the subscription to /api/push/subscribe"
    - "Receiving a push shows a system notification; tapping it opens the CRM at the relevant thread"
  artifacts:
    - path: "public/sw.js"
      provides: "push event handler (showNotification) + notificationclick handler (openWindow)"
      contains: "addEventListener"
      min_lines: 25
    - path: "app/(crm)/admin/(protected)/kit/push-subscribe.jsx"
      provides: "client subscribe row with state machine + standalone gate"
      contains: "pushManager.subscribe"
    - path: "app/(crm)/admin/(protected)/kit/app.jsx"
      provides: "service worker registration via useEffect"
      contains: "serviceWorker.register"
  key_links:
    - from: "app/(crm)/admin/(protected)/kit/app.jsx"
      to: "public/sw.js"
      via: "navigator.serviceWorker.register('/sw.js') in useEffect"
      pattern: "serviceWorker\\.register"
    - from: "app/(crm)/admin/(protected)/kit/push-subscribe.jsx"
      to: "/api/push/subscribe"
      via: "fetch POST with subscription + teamMemberId"
      pattern: "/api/push/subscribe"
    - from: "public/sw.js notificationclick"
      to: "CRM thread URL"
      via: "clients.openWindow(data.url)"
      pattern: "openWindow"
---

<objective>
Build the client layer: a service worker that renders push notifications and routes taps to the right thread, service-worker registration in the CRM shell, and a subscribe row in the Settings screen that drives the full permission to subscribe to persist flow.

Purpose: This completes the end-to-end push loop. Plan 10-02 dispatches notifications server-side; this plan lets a team member subscribe (NOTIF-01), receive the OS notification (NOTIF-02 client half), and tap through to the conversation (NOTIF-03). After this plan plus VAPID keys in env, the phase is functionally complete.
Output: public/sw.js, a push-subscribe client component, SW registration in app.jsx, and a subscribe row wired into screens-settings.jsx, followed by a human-verify checkpoint on a real installed PWA.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-web-push-notifications/10-CONTEXT.md
@.planning/phases/10-web-push-notifications/10-RESEARCH.md
@.planning/phases/10-web-push-notifications/10-UI-SPEC.md

@app/(crm)/admin/(protected)/kit/screens-settings.jsx
@app/(crm)/admin/(protected)/kit/app.jsx

<interfaces>
<!-- Verified contracts and the locked payload/key shapes from Plan 10-02. -->

Subscribe API contract (from Plan 10-02 — app/api/push/subscribe/route.js):
  POST /api/push/subscribe
  body: { subscription: { endpoint, keys: { p256dh, auth } }, teamMemberId: "<wa_id>" }
  -> 200 { ok: true } | 400 invalid | 401 unauthorized | 503 kv unavailable
IMPORTANT: teamMemberId == the team member's wa_id (the value stored as push:sub:{wa_id} in KV).
The client must send the wa_id it is responsible for.

Push payload shape the SW receives (from Plan 10-02 dispatchToTeam, JSON.stringify'd):
  { "title": "New WhatsApp", "body": "{leadName}: {preview}", "data": { "threadId": "uuid", "url": "/admin/whatsapp?thread=uuid" } }

Env var available client-side (from Plan 10-01):
  NEXT_PUBLIC_VAPID_PUBLIC_KEY  — base64url VAPID public key for applicationServerKey

Base64url to Uint8Array helper (inline, do NOT add a package — 10-RESEARCH.md Pattern 3):
  function urlBase64ToUint8Array(base64String) {
    const padding = "=".repeat((4 - (base64String.length % 4)) % 4);
    const base64 = (base64String + padding).replace(/-/g, "+").replace(/_/g, "/");
    const rawData = atob(base64);
    return Uint8Array.from([...rawData].map((c) => c.charCodeAt(0)));
  }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create public/sw.js — push + notificationclick handlers</name>
  <files>public/sw.js</files>
  <action>
    Create public/sw.js (NOTIF-02 client half + NOTIF-03). Vanilla service worker JS — no imports, no bundler.

    push handler:
      self.addEventListener("push", (event) => {
        const data = event.data?.json() ?? {};
        const title = data.title ?? "New WhatsApp";
        const options = {
          body: data.body ?? "",
          data: data.data ?? {},
          icon: "/icon-192.png",
          badge: "/icon-192.png",
          tag: data.data?.threadId,
        };
        event.waitUntil(self.registration.showNotification(title, options));
      });

    If /icon-192.png does not exist in public/, check public/ for an existing icon (favicon or app icon) and use that path, or omit icon/badge entirely. Do not reference a missing asset that breaks the notification.

    notificationclick handler (NOTIF-03 — open the relevant thread):
      self.addEventListener("notificationclick", (event) => {
        event.notification.close();
        const url = event.notification.data?.url ?? "/admin/whatsapp";
        event.waitUntil(
          clients.matchAll({ type: "window", includeUncontrolled: true }).then((list) => {
            for (const client of list) {
              if (client.url.includes("/admin") && "focus" in client) {
                client.focus();
                client.postMessage({ type: "navigate", url });
                return;
              }
            }
            return clients.openWindow(url);
          })
        );
      });

    The url comes from the locked payload data.url (e.g. /admin/whatsapp?thread={threadId}). The /admin/whatsapp tab itself is built in Phase 11 — for Phase 10 the openWindow target is correct even though it lands on the base admin route until Phase 11 ships. That is expected and acceptable.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('public/sw.js','utf8'); const ok=s.includes('addEventListener(\"push\"')&&s.includes('showNotification')&&s.includes('addEventListener(\"notificationclick\"')&&s.includes('openWindow'); if(!ok)process.exit(1); console.log('sw.js OK')"</automated>
  </verify>
  <done>public/sw.js has a push handler that calls showNotification with the payload, and a notificationclick handler that focuses an existing admin window (postMessage navigate) or openWindow's the thread URL.</done>
</task>

<task type="auto">
  <name>Task 2: Register the service worker in the CRM shell (app.jsx)</name>
  <files>app/(crm)/admin/(protected)/kit/app.jsx</files>
  <action>
    Register the service worker (NOTIF-01 prerequisite — pushManager.subscribe requires an active SW registration). app.jsx is already "use client" and already uses useEffect (10-RESEARCH.md Pattern 5, Option 2 — lowest effort; the protected layout is a Server Component and cannot do this).

    Add a useEffect that registers /sw.js:
      useEffect(() => {
        if (typeof navigator === "undefined" || !("serviceWorker" in navigator)) return;
        navigator.serviceWorker.register("/sw.js").catch(console.error);
      }, []);

    Also handle the navigate postMessage from the SW (so notificationclick focusing an existing window actually navigates):
      useEffect(() => {
        if (typeof navigator === "undefined" || !("serviceWorker" in navigator)) return;
        const onMsg = (e) => {
          if (e.data?.type === "navigate" && e.data.url) window.location.assign(e.data.url);
        };
        navigator.serviceWorker.addEventListener("message", onMsg);
        return () => navigator.serviceWorker.removeEventListener("message", onMsg);
      }, []);

    Match the file's existing import style and hook placement. Do NOT register the SW in the Server Component layout — navigator is undefined during RSC render (10-RESEARCH.md Pitfall 5).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('app/(crm)/admin/(protected)/kit/app.jsx','utf8'); const ok=s.includes('serviceWorker.register')&&s.includes('/sw.js'); if(!ok)process.exit(1); console.log('SW registration OK')"</automated>
  </verify>
  <done>app.jsx registers /sw.js in a useEffect guarded for non-SW environments and listens for the SW navigate postMessage. No SSR/RSC navigator access.</done>
</task>

<task type="auto">
  <name>Task 3: Build push-subscribe.jsx and wire it into Settings</name>
  <files>app/(crm)/admin/(protected)/kit/push-subscribe.jsx, app/(crm)/admin/(protected)/kit/screens-settings.jsx</files>
  <action>
    Create the subscribe row component and add it to the INTEGRATIONS section of the Settings screen (NOTIF-01). Follow 10-UI-SPEC.md EXACTLY — this is the locked UI design contract.

    Create app/(crm)/admin/(protected)/kit/push-subscribe.jsx ("use client"):

    State machine (per UI-SPEC Component Inventory — exact state names):
    checking | unsupported | unsubscribed | requesting | subscribed | error  (track an errorMsg string for the error state).

    Standalone gate (UI-SPEC "Standalone Gate Implementation") — render null outside an installed PWA:
      const [isStandalone, setIsStandalone] = useState(false);
      useEffect(() => { setIsStandalone(window.matchMedia("(display-mode: standalone)").matches); }, []);
      if (!isStandalone) return null;

    On mount (after standalone confirmed): if !("serviceWorker" in navigator) || !("PushManager" in window) -> state unsupported. Else check Notification.permission: "granted" plus an existing pushManager.getSubscription() -> subscribed; otherwise unsubscribed. Start in checking, resolve within ~200ms.

    Subscribe handler (must run INSIDE the click gesture — iOS requirement, 10-RESEARCH.md Pitfall 6):
    1. setState requesting.
    2. const permission = await Notification.requestPermission(); if not "granted" -> state error, errorMsg "PERMISSION DENIED BY USER", console.warn (no custom alert per UI-SPEC copywriting), return.
    3. const reg = await navigator.serviceWorker.ready;
    4. const sub = await reg.pushManager.subscribe({ userVisibleOnly: true, applicationServerKey: urlBase64ToUint8Array(process.env.NEXT_PUBLIC_VAPID_PUBLIC_KEY) });  (userVisibleOnly:true is mandatory — false throws, 10-RESEARCH.md Pitfall 4). Include the inline urlBase64ToUint8Array helper from the interfaces block.
    5. POST to the subscribe API:
         const res = await fetch("/api/push/subscribe", {
           method: "POST",
           headers: { "Content-Type": "application/json" },
           body: JSON.stringify({ subscription: sub.toJSON(), teamMemberId })
         });
         if (!res.ok) throw new Error(`subscribe failed: ${res.status}`);
       teamMemberId: source the current team member's wa_id. Check how app.jsx / the settings screen already knows the logged-in member (session/index data). If a wa_id is not readily available in existing client state, accept a prop teamMemberId passed from screens-settings.jsx; if that screen also lacks it, fall back to the admin session username as the identifier and document the gap in the SUMMARY (the KV key would then be push:sub:{username}). Do NOT block the build on this — pick the best available identifier and record the choice. (10-RESEARCH.md Open Question 1: wa_id is preferred.)
    6. On success -> state subscribed. On any throw -> state error, errorMsg = error.message uppercased, truncated to 48 chars (UI-SPEC).

    Render a single .set-row using the existing CRM class pattern (set-row, icon circle, lbl > name + meta). Use Icon.send (UI-SPEC icon decision). Apply the exact per-state visuals from the UI-SPEC States table:
    - icon bg, optional 3px left border (3px solid #1F4FFF only when subscribed), row name copy, meta copy, chevron visibility — all per the UI-SPEC States table and Copywriting Contract.
    - subscribed meta = a 6px #27AE60 dot + "ACTIVE". No onClick when subscribed (cursor default, opacity 0.9 — no unsubscribe this phase).
    - error and unsubscribed states remain tappable (retry).
    Use ONLY existing color tokens / classes named in the UI-SPEC. Do not introduce new type sizes or colors. No animations beyond the existing .screen fadeIn.

    Then edit app/(crm)/admin/(protected)/kit/screens-settings.jsx: import the new component and render it (e.g. <PushSubscribeRow />) inside the INTEGRATIONS section adjacent to the existing Telegram row. The component self-gates to null outside standalone, so the section layout is unaffected when hidden. Match the existing row insertion style in that section.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('app/(crm)/admin/(protected)/kit/push-subscribe.jsx','utf8'); const s=fs.readFileSync('app/(crm)/admin/(protected)/kit/screens-settings.jsx','utf8'); const okc=c.includes('pushManager.subscribe')&&c.includes('userVisibleOnly: true')&&c.includes('/api/push/subscribe')&&c.includes('display-mode: standalone'); const oks=/PushSubscribe|push-subscribe/.test(s); if(!okc||!oks){console.error({okc,oks});process.exit(1)} console.log('subscribe UI OK')"</automated>
  </verify>
  <done>push-subscribe.jsx renders the standalone-gated subscribe row with all 6 UI-SPEC states, runs the permission/subscribe/POST flow inside the click gesture, and is imported + rendered in the INTEGRATIONS section of screens-settings.jsx.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify end-to-end push on installed PWA</name>
  <files>none — verification only</files>
  <action>
    This is a human verification checkpoint. Claude has already built public/sw.js, SW registration in app.jsx, and the standalone-gated subscribe row in CRM Settings. Combined with Plan 10-02's server dispatch, this is the full inbound-WhatsApp to phone-notification loop. The user verifies it live on an installed PWA (see how-to-verify). Do not write code in this task — pause and wait for the resume signal.
  </action>
  <what-built>
    Service worker (public/sw.js), SW registration in app.jsx, and the standalone-gated subscribe row in CRM Settings. Combined with Plan 10-02's server dispatch, this is the full inbound-WhatsApp to phone-notification loop.
  </what-built>
  <how-to-verify>
    Prerequisite: VAPID keys (from Plan 10-01) must be set in .env.local AND Vercel (VAPID_PUBLIC_KEY, VAPID_PRIVATE_KEY, NEXT_PUBLIC_VAPID_PUBLIC_KEY — public + NEXT_PUBLIC copies identical). Deploy to a real HTTPS URL (push does not work on http, and iOS requires an installed PWA).

    1. On a phone, open the deployed CRM in the browser, then Add to Home Screen (install the PWA). Open it from the home screen icon (standalone mode).
    2. Go to Settings -> INTEGRATIONS. Confirm the "Enable notifications" row IS visible. (Then open the same CRM in a normal browser tab and confirm the row is ABSENT — standalone gate works.)
    3. Tap the row. Grant the OS permission prompt. Confirm the row transitions to "Notifications / ● ACTIVE" (blue left border, green dot).
    4. Send a WhatsApp message to the team test number. Within ~5 seconds, confirm a system push notification appears on the phone titled "New WhatsApp" with the lead name + preview.
    5. Tap the notification. Confirm the CRM opens (to /admin/whatsapp?thread=... — note the WhatsApp tab itself ships in Phase 11; for now landing on the admin app is sufficient proof the URL routed).
    6. Telegram fallback: from a second team member account that has NOT subscribed, confirm that the same inbound WhatsApp produced a Telegram message in the M&C chat instead.

    Expected: push within 5s on the subscribed device (NOTIF-02), tap opens CRM (NOTIF-03), Telegram fires for the unsubscribed member (NOTIF-04), row only shows in standalone (NOTIF-01).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what failed (e.g. "no notification arrived", "row showed in browser tab", "tap did nothing").</resume-signal>
</task>

</tasks>

<verification>
- public/sw.js handles push (showNotification) and notificationclick (focus/openWindow).
- app.jsx registers /sw.js and relays SW navigate messages.
- push-subscribe.jsx implements the 6-state UI-SPEC machine, the standalone gate, and the permission/subscribe/POST flow inside the click gesture.
- screens-settings.jsx renders the row in INTEGRATIONS.
- Human checkpoint confirms the live end-to-end loop and Telegram fallback.
</verification>

<success_criteria>
- NOTIF-01: subscribe row visible only in installed PWA standalone mode; subscribing persists via /api/push/subscribe.
- NOTIF-02: a real inbound WhatsApp produces a system notification on the subscribed device.
- NOTIF-03: tapping the notification opens the CRM at the thread URL.
- Phase 10 is functionally complete after this checkpoint passes (pending VAPID keys in env).
</success_criteria>

<output>
After completion, create `.planning/phases/10-web-push-notifications/10-03-SUMMARY.md`.
Record: the teamMemberId identifier actually used (wa_id vs username) and where it was sourced, the SW registration location, and whether /icon-192.png existed or an alternative icon path was used.
</output>
