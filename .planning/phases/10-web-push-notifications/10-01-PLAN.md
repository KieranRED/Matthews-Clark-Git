---
phase: 10-web-push-notifications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .env.example
autonomous: true
requirements: [NOTIF-01]
user_setup:
  - service: web-push (VAPID)
    why: "Web Push requires a VAPID keypair shared across all deployments; keys are secrets that Claude generates locally but the user must add to Vercel"
    env_vars:
      - name: VAPID_PUBLIC_KEY
        source: "Generated by: node -e \"const wp=require('web-push'); console.log(JSON.stringify(wp.generateVAPIDKeys(),null,2))\" — add to Vercel + .env.local"
      - name: VAPID_PRIVATE_KEY
        source: "Same generation command (private half of the keypair) — add to Vercel + .env.local"
      - name: NEXT_PUBLIC_VAPID_PUBLIC_KEY
        source: "Same value as VAPID_PUBLIC_KEY (browser-readable copy) — add to Vercel + .env.local"

must_haves:
  truths:
    - "web-push@^3.6.7 is installed and importable in a nodejs-runtime route"
    - "A developer can generate a VAPID keypair with a documented one-liner"
    - ".env.example lists VAPID_PUBLIC_KEY, VAPID_PRIVATE_KEY, NEXT_PUBLIC_VAPID_PUBLIC_KEY with guidance"
  artifacts:
    - path: "package.json"
      provides: "web-push dependency declaration"
      contains: "web-push"
    - path: ".env.example"
      provides: "VAPID env var documentation"
      contains: "VAPID_PUBLIC_KEY"
  key_links:
    - from: "package.json"
      to: "node_modules/web-push"
      via: "npm install"
      pattern: "web-push"
---

<objective>
Install the `web-push` library and document the VAPID environment variables that every other Phase 10 plan depends on.

Purpose: `web-push` provides the VAPID crypto (ECDH + JWT signing) required to send browser push notifications server-side. It is not yet in package.json. The VAPID keypair must be consistent across all Vercel deployments — changing it invalidates every subscription — so the env vars are documented here as the single source of truth.
Output: web-push in package.json, three VAPID env vars documented in .env.example, and a generation one-liner so the user can mint keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-web-push-notifications/10-CONTEXT.md
@.planning/phases/10-web-push-notifications/10-RESEARCH.md

This is a Wave 0 infrastructure plan. No source code is written — only dependency install and env documentation. Plans 10-02 (server dispatch) and 10-03 (client subscribe) both import/use what this plan establishes.

Verified facts (from 10-RESEARCH.md):
- `web-push@3.6.7` is the current latest and the locked version (`npm view web-push version` → 3.6.7).
- VAPID subject must be a `mailto:` address, NOT `https://localhost` — Safari/iOS rejects localhost with `BadJwtToken`. Use `mailto:kierandeclanredpath@gmail.com`.
- The browser subscribe flow (Plan 10-03) reads `NEXT_PUBLIC_VAPID_PUBLIC_KEY` (same value as `VAPID_PUBLIC_KEY`, just the NEXT_PUBLIC_ prefixed copy so it's bundled client-side).
- Server send (Plan 10-02) reads `VAPID_PUBLIC_KEY` + `VAPID_PRIVATE_KEY`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install web-push@^3.6.7</name>
  <files>package.json, package-lock.json</files>
  <action>
    Run `npm install web-push@^3.6.7` from the repo root. This is the locked dependency (10-CONTEXT.md decision; 10-RESEARCH.md confirms 3.6.7 is latest). Do NOT install any alternative push library — web-push is the only mature Node VAPID library and is explicitly locked.

    After install, confirm `web-push` appears under `dependencies` (not devDependencies) in package.json, and that package-lock.json was updated.

    Do not modify any other dependencies.
  </action>
  <verify>
    <automated>node -e "require('web-push'); console.log('web-push OK')" && node -e "const p=require('./package.json'); if(!p.dependencies['web-push']) process.exit(1); console.log('dep present:', p.dependencies['web-push'])"</automated>
  </verify>
  <done>web-push resolves via require() and is listed under package.json dependencies at a ^3.6.7-compatible version.</done>
</task>

<task type="auto">
  <name>Task 2: Document VAPID env vars + generation one-liner in .env.example</name>
  <files>.env.example</files>
  <action>
    Add a "Web Push (Phase 10)" section to .env.example with three variables and a comment block explaining how to generate them. Do NOT put real key values in .env.example — only placeholders and instructions (real keys live in .env.local and Vercel, never committed).

    Append this block (adapt comment style to match the existing .env.example conventions):

    ```
    # ── Web Push notifications (Phase 10) ──────────────────────────────
    # Generate a VAPID keypair ONCE and reuse it across ALL deployments.
    # Changing these invalidates every existing push subscription.
    #   node -e "const wp=require('web-push'); console.log(JSON.stringify(wp.generateVAPIDKeys(),null,2))"
    # Add the SAME public value to both VAPID_PUBLIC_KEY and NEXT_PUBLIC_VAPID_PUBLIC_KEY.
    # VAPID subject is a mailto: address (NOT https://localhost — Safari/iOS rejects it).
    VAPID_PUBLIC_KEY=
    VAPID_PRIVATE_KEY=
    NEXT_PUBLIC_VAPID_PUBLIC_KEY=
    ```

    Per NOTIF-01: these env vars are the foundation for the subscribe flow. The NEXT_PUBLIC_ copy is required because the browser subscribe code (Plan 10-03) needs the public key at build time and cannot read non-prefixed env vars.
  </action>
  <verify>
    <automated>grep -q "VAPID_PUBLIC_KEY" .env.example && grep -q "VAPID_PRIVATE_KEY" .env.example && grep -q "NEXT_PUBLIC_VAPID_PUBLIC_KEY" .env.example && grep -q "generateVAPIDKeys" .env.example && echo ".env.example VAPID block OK"</automated>
  </verify>
  <done>.env.example contains all three VAPID variables (empty placeholders) plus the generation one-liner and the mailto/localhost warning comment.</done>
</task>

</tasks>

<verification>
- `require('web-push')` succeeds in a Node context (nodejs runtime is what the webhook + subscribe routes use).
- package.json declares web-push under dependencies.
- .env.example documents VAPID_PUBLIC_KEY, VAPID_PRIVATE_KEY, NEXT_PUBLIC_VAPID_PUBLIC_KEY with the generation one-liner.
- No real key material is committed.
</verification>

<success_criteria>
- web-push@^3.6.7 installed and importable (NOTIF-01 infra prerequisite).
- VAPID env vars documented so the user can generate keys and add them to Vercel.
- Plans 10-02 and 10-03 can now import web-push and reference the env vars without re-establishing infrastructure.
</success_criteria>

<output>
After completion, create `.planning/phases/10-web-push-notifications/10-01-SUMMARY.md`.
Note in the summary: the user must run the generation one-liner and add the three keys to .env.local + Vercel before any end-to-end push test can succeed (this is the user_setup item — execute-plan will surface it).
</output>
