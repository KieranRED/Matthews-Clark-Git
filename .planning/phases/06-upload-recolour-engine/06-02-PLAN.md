---
phase: 06-upload-recolour-engine
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - public/wrap-studio/stage.jsx
  - public/wrap-studio/studio.css
autonomous: true
requirements: [UPLOAD-01, RCOL-01, RCOL-02, RCOL-03, RCOL-04, RCOL-05, RCOL-06, RCOL-07, RCOL-08]
must_haves:
  truths:
    - "Customer can upload a .heic photo and it converts to JPEG before background removal, then shows a masked car"
    - "Selecting a metallic swatch shows a subtle grain texture on the car that gloss of the same hue does not"
    - "Each of the 9 finish types (gloss, satin, matte, chrome, shift, metallic, carbon, ppf-clear, ppf-matte) renders a distinct preview on the masked cutout"
  artifacts:
    - path: "public/wrap-studio/stage.jsx"
      provides: "heic2any lazy-load + conversion; metallic & carbon cases in fxFor; SVG metallic-noise filter; noise filter applied to tint"
      contains: "metallic-noise"
    - path: "public/wrap-studio/studio.css"
      provides: "no new class strictly required; carbon texture support if used"
  key_links:
    - from: "stage.jsx ingest()"
      to: "window.heic2any"
      via: "lazy script-tag load when .heic/.heif extension"
      pattern: "heic2any"
    - from: "fxFor metallic case"
      to: "#metallic-noise SVG filter"
      via: "fx.noise flag drives filter: url(#metallic-noise) on tint layer"
      pattern: "url\\(#metallic-noise\\)"
---

<objective>
Add HEIC support to the ingest pipeline and complete the recolour finish set: add `metallic` and `carbon` cases to `fxFor()`, define the SVG `feTurbulence` noise filter once in the stage DOM, and apply it to the tint layer for the metallic finish.

Purpose: Closes the upload format gap (HEIC) and the finish-fidelity gap (metallic grain + carbon), so all 9 catalogue finishes preview correctly on the bg-removed cutout.
Output: HEIC upload works; all 9 finishes render distinctly with metallic showing flake-depth grain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-upload-recolour-engine/06-UI-SPEC.md
@.planning/phases/06-upload-recolour-engine/06-RESEARCH.md
@public/wrap-studio/stage.jsx

NOTE: This plan depends on 06-01 having already replaced `ingest()` with the async pipeline (which contains the comment `// HEIC conversion injected here by Plan 06-02`).

The catalogue contains a `carbon` finish (e.g. ids `avery-carbon-*`, `hexis-carbon-*`) that currently falls through to `fxFor` default. Add an explicit `carbon` case so it reads as a textured matte-ish carbon look rather than plain gloss.

<interfaces>
fxFor switch already handles: gloss, satin, matte, chrome, shift, ppf-clear, ppf-matte, default.
fxLayers currently renders: tone, tint (`car-fx car-tint ${fx.anim||''}`), sheen. The tint div spreads `...maskStyle, ...clip, ...fx.tint`.
heic2any v0.0.4 UMD CDN: https://cdn.jsdelivr.net/npm/heic2any@0.0.4/dist/heic2any.min.js — sets window.heic2any. API: `await window.heic2any({ blob, toType:'image/jpeg', quality:0.92 })` returns Blob | Blob[].
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HEIC lazy-load + conversion in ingest pipeline</name>
  <read_first>
    - public/wrap-studio/stage.jsx
  </read_first>
  <files>public/wrap-studio/stage.jsx</files>
  <action>
    Add a module-scope lazy loader alongside `loadBgRemoval`:
    ```
    let _heicReady = null;
    function loadHeic2Any() {
      if (window.heic2any) return Promise.resolve();
      if (_heicReady) return _heicReady;
      _heicReady = new Promise((resolve, reject) => {
        const s = document.createElement('script');
        s.src = 'https://cdn.jsdelivr.net/npm/heic2any@0.0.4/dist/heic2any.min.js';
        s.onload = () => resolve();
        s.onerror = () => reject(new Error('Failed to load HEIC converter'));
        document.head.appendChild(s);
      });
      return _heicReady;
    }
    ```
    In `ingest()`, replace the placeholder line `// HEIC conversion injected here by Plan 06-02` with:
    ```
    if (isHeic) {
      await loadHeic2Any();
      const out = await window.heic2any({ blob: file, toType: 'image/jpeg', quality: 0.92 });
      processFile = Array.isArray(out) ? out[0] : out;
    }
    ```
    (`isHeic` and `processFile` already exist in the 06-01 ingest body. heic2any output then flows into `resizeIfLarge`.)
  </action>
  <verify>
    <automated>grep -q "loadHeic2Any" public/wrap-studio/stage.jsx && grep -q "heic2any" public/wrap-studio/stage.jsx && grep -q "toType: 'image/jpeg'" public/wrap-studio/stage.jsx && echo PASS</automated>
    <manual>Upload a .heic file → converts and shows masked car without error.</manual>
  </verify>
  <done>heic2any loads only when a .heic/.heif file is selected, converts to JPEG, and the result feeds the existing resize + removal pipeline.</done>
</task>

<task type="auto">
  <name>Task 2: Metallic + carbon fxFor cases, SVG noise filter, apply to tint</name>
  <read_first>
    - public/wrap-studio/stage.jsx
    - .planning/phases/06-upload-recolour-engine/06-UI-SPEC.md
  </read_first>
  <files>public/wrap-studio/stage.jsx, public/wrap-studio/studio.css</files>
  <action>
    1. In `fxFor()` switch, add two cases before `default`:
    ```
    case 'metallic':
      return { tint: { background: c, mixBlendMode: 'color', opacity: .96 },
               sheen: { opacity: .22 }, tone: { opacity: .18 }, noise: true };
    case 'carbon':
      return { tint: { background: c, mixBlendMode: 'color', opacity: 1 },
               sheen: { opacity: .08 }, tone: { opacity: .38 } };
    ```

    2. In `fxLayers`, change the tint div so the noise filter applies only when `fx.noise` is set:
    ```
    h('div', { key: 'tint', className: 'car-fx car-tint ' + (fx.anim || ''),
      style: { ...maskStyle, ...clip, ...fx.tint, filter: fx.noise ? 'url(#metallic-noise)' : undefined } }),
    ```

    3. Render the SVG noise filter ONCE inside the `.stage` element (add as a child after the `bay-floor`/before `car-wrap`, only matters that it's in the DOM). Use exactly the UI-SPEC definition:
    ```
    h('svg', { style: { position: 'absolute', width: 0, height: 0 }, 'aria-hidden': 'true' },
      h('defs', null,
        h('filter', { id: 'metallic-noise', x: '0%', y: '0%', width: '100%', height: '100%' },
          h('feTurbulence', { type: 'fractalNoise', baseFrequency: '0.72', numOctaves: '4', seed: '2', result: 'noise' }),
          h('feColorMatrix', { type: 'saturate', values: '0', in: 'noise', result: 'grey' }),
          h('feBlend', { in: 'SourceGraphic', in2: 'grey', mode: 'multiply', result: 'blended' }),
          h('feComponentTransfer', null, h('feFuncA', { type: 'linear', slope: '1' }))))),
    ```
    Do NOT set baseFrequency outside 0.60–0.80. Do not additionally reduce tint opacity beyond the .96 already in the metallic case (per UI-SPEC pitfall — avoid double-darkening).

    No new studio.css class is strictly required; if needed for carbon, you may add a subtle repeating texture but keep it optional. studio.css is in files_modified only as a guard — leave unchanged if not needed.
  </action>
  <verify>
    <automated>grep -q "case 'metallic'" public/wrap-studio/stage.jsx && grep -q "case 'carbon'" public/wrap-studio/stage.jsx && grep -q "metallic-noise" public/wrap-studio/stage.jsx && grep -q "feTurbulence" public/wrap-studio/stage.jsx && grep -q "url(#metallic-noise)" public/wrap-studio/stage.jsx && echo PASS</automated>
    <manual>With a car uploaded, cycle through all 9 finishes. Metallic shows subtle grain vs gloss of same hue; carbon reads darker/flatter than gloss; chrome/shift animate; matte flat; ppf thin tint.</manual>
  </verify>
  <done>fxFor handles metallic (with noise flag) and carbon; the metallic-noise SVG filter exists in the stage DOM and is applied to the tint layer only when finish is metallic; all 9 finishes render distinctly.</done>
</task>

</tasks>

<verification>
- .heic upload converts and reaches the masked-car state.
- All 9 finish types render distinct previews on the cutout.
- Metallic grain visible and subtle; carbon distinct from gloss.
</verification>

<success_criteria>
UPLOAD-01 (HEIC), RCOL-01..08 (all finish treatments including metallic noise) verifiable in browser.
</success_criteria>

<output>
After completion, create `.planning/phases/06-upload-recolour-engine/06-02-SUMMARY.md`
</output>
