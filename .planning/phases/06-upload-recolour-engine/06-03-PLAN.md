---
phase: 06-upload-recolour-engine
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - public/wrap-studio/stage.jsx
  - public/wrap-studio/studio.css
autonomous: true
requirements: [RCOL-09, RCOL-10]
must_haves:
  truths:
    - "Customer can drag the before/after slider and see the original photo (with its real background) on the before side and the wrapped cutout on the after side"
    - "The before/after Compare button is disabled (dimmed, non-interactive) until originalUrl is set"
    - "Customer can assign different colours to individual panels and each panel holds its own colour on the masked cutout"
  artifacts:
    - path: "public/wrap-studio/stage.jsx"
      provides: "car-base--original img wired behind clipped layers; Compare button disabled-until-originalUrl"
      contains: "car-base--original"
    - path: "public/wrap-studio/studio.css"
      provides: ".car-base--original positioning rule"
      contains: ".car-base--original"
  key_links:
    - from: "stage.jsx before/after render"
      to: "originalUrl"
      via: "car-base--original img at z-index 0, unclipped, behind clipped car-base + fxLayers"
      pattern: "car-base--original"
    - from: "Compare pill-btn"
      to: "originalUrl null check"
      via: "opacity .4 / pointer-events none when originalUrl is null"
      pattern: "originalUrl"
---

<objective>
Wire `originalUrl` into the before/after slider so the "before" side shows the real photo with its background, gate the Compare toggle on `originalUrl`, and confirm per-panel colour assignment works on the bg-removed cutout.

Purpose: Delivers the before/after comparison (RCOL-10) and verifies per-panel assignment (RCOL-09) against the new masked-cutout pipeline.
Output: A working before/after slider (original vs wrapped) and confirmed per-panel recolour.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-upload-recolour-engine/06-UI-SPEC.md
@public/wrap-studio/stage.jsx

<interfaces>
Existing stage.jsx before/after machinery (already built):
- `baActive`, `setBaActive` props; `baPos` state; drag handlers; `clip = baActive ? { clipPath: inset(0 ${100-baPos}% 0 0), ... } : null`
- car loaded render: `h('img', { className: 'car-base', src: carUrl, alt: 'Your car' })` then `fxLayers`
- `.car-base` is z-index 1; `.car-fx` is z-index 2 (studio.css).
- ba-tags "Wrapped" (after) and "Original" (before) and `.ba-divider` already rendered when `baActive && colored`.
- bottom-right HUD has the Compare pill-btn: `h('button', { className: 'pill-btn' + (baActive ? ' on' : ''), title: 'Before / after', onClick: () => setBaActive(!baActive) }, h(I.Compare, { size: 16 }))`

NOTE: `clip` must apply to `.car-base` (z1) and fxLayers (z2) but NOT to `.car-base--original` (z0). Per UI-SPEC, the original sits unclipped behind so it reveals on the left as the clipped layers slide right.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire car-base--original into before/after + gate Compare button</name>
  <read_first>
    - public/wrap-studio/stage.jsx
    - public/wrap-studio/studio.css
    - .planning/phases/06-upload-recolour-engine/06-UI-SPEC.md
  </read_first>
  <files>public/wrap-studio/stage.jsx, public/wrap-studio/studio.css</files>
  <action>
    1. Apply `clip` to the loaded `.car-base` img (it currently has no clip). Change:
    `h('img', { className: 'car-base', src: carUrl, alt: 'Your car' })`
    to:
    `h('img', { className: 'car-base', src: carUrl, style: clip, alt: 'Your car' })`
    (fxLayers already spread `...clip`.)

    2. In the carUrl-loaded fragment, render the original image FIRST (so it sits behind at z-index 0), only when originalUrl exists:
    ```
    carUrl
      ? h(React.Fragment, null,
          originalUrl ? h('img', { className: 'car-base car-base--original', src: originalUrl, alt: 'Original car' }) : null,
          h('img', { className: 'car-base', src: carUrl, style: clip, alt: 'Your car' }),
          fxLayers)
      : ...
    ```

    3. Gate the Compare pill-btn on originalUrl. Replace its render with:
    ```
    h('button', {
      className: 'pill-btn' + (baActive ? ' on' : ''),
      title: 'Before / after',
      'aria-label': 'Toggle before/after comparison',
      style: originalUrl ? null : { opacity: 0.4, cursor: 'default', pointerEvents: 'none' },
      onClick: () => { if (originalUrl) setBaActive(!baActive); }
    }, h(I.Compare, { size: 16 }))
    ```

    In `public/wrap-studio/studio.css`, add (after the `.car-base` rule):
    ```
    .car-base--original{position:absolute;inset:0;width:100%;height:100%;object-fit:contain;z-index:0}
    ```
    (.car-base is z-index 1; .car-fx is z-index 2 — original stays behind, unclipped.)
  </action>
  <verify>
    <automated>grep -q "car-base--original" public/wrap-studio/stage.jsx && grep -q "Toggle before/after comparison" public/wrap-studio/stage.jsx && grep -q ".car-base--original" public/wrap-studio/studio.css && grep -q "z-index:0" public/wrap-studio/studio.css && echo PASS</automated>
    <manual>Upload a car, select a colour, click Compare, drag slider: left shows original photo WITH background, right shows wrapped cutout. Before upload, Compare button is dimmed and non-clickable.</manual>
  </verify>
  <done>Before/after slider shows original (with background) on the before side and wrapped cutout on the after side; Compare button is disabled until originalUrl is set and carries the aria-label.</done>
</task>

<task type="auto">
  <name>Task 2: Verify per-panel colour assignment on the masked cutout</name>
  <read_first>
    - public/wrap-studio/stage.jsx
    - public/wrap-studio/app.jsx
  </read_first>
  <files>public/wrap-studio/stage.jsx</files>
  <action>
    RCOL-09 (per-panel assignment) is already wired in app.jsx (`panelColors`, `activePanel`, `onSelect` writes the swatch to the active panel). Confirm it still functions correctly with the bg-removed cutout and make NO functional change unless a real defect is found.

    Verification-only task: trace the panel-chip flow (`.pchip` buttons set `activePanel`; `onSelect` assigns to `panelColors[activePanel]`). If the chips render and selecting a swatch updates the active panel's stored colour with the cutout displayed, the requirement is met.

    If (and only if) you find the displayed stage swatch does not reflect the active panel selection, document the defect in the SUMMARY rather than silently rewriting the engine — per-panel rendering scope is established prototype behaviour.
  </action>
  <verify>
    <automated>grep -q "panelColors" public/wrap-studio/app.jsx && grep -q "activePanel" public/wrap-studio/stage.jsx && echo PASS</automated>
    <manual>Set activePanel to Bonnet, pick a colour; switch to Full body, pick another — each panel chip retains its own swatch colour and the cap/preview reflect the active selection.</manual>
  </verify>
  <done>Per-panel assignment confirmed working with the masked cutout; chips hold independent colours. Any defect found is documented, not silently changed.</done>
</task>

</tasks>

<verification>
- Before/after slider reveals original (with background) vs wrapped cutout.
- Compare disabled until originalUrl present.
- Per-panel colours hold independently.
</verification>

<success_criteria>
RCOL-09 (per-panel assignment) and RCOL-10 (before/after slider) verifiable in browser.
</success_criteria>

<output>
After completion, create `.planning/phases/06-upload-recolour-engine/06-03-SUMMARY.md`
</output>
