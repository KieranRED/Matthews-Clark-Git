---
phase: 06-upload-recolour-engine
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified: []
autonomous: false
requirements: [UPLOAD-01, UPLOAD-02, UPLOAD-03, UPLOAD-04, RCOL-01, RCOL-02, RCOL-03, RCOL-04, RCOL-05, RCOL-06, RCOL-07, RCOL-08, RCOL-09, RCOL-10]
must_haves:
  truths:
    - "All 4 phase success criteria confirmed in a real browser"
    - "All 14 Phase 6 requirements observably met"
  artifacts: []
  key_links: []
---

<objective>
User Acceptance Testing for Phase 6. Verify all 4 success criteria and 14 requirements in the browser before marking the phase complete.

Purpose: Final human gate — visual and functional behaviours that cannot be auto-verified.
Output: UAT sign-off (or documented gaps for a gap-closure plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-upload-recolour-engine/06-VALIDATION.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Phase 6 UAT walkthrough</name>
  <files>none — verification only</files>
  <action>Present the verification walkthrough below to the user and pause for sign-off. This is a human-verify checkpoint; no code is written.</action>
  <what-built>
    In-browser background-removal ingest pipeline (JPG/PNG/HEIC), pre-resize, progress + error UI, originalUrl/carUrl persistence with quota guard, COOP/COEP headers, metallic noise + carbon finishes, full 9-finish recolour on the cutout, before/after slider wired to originalUrl, and per-panel colour assignment.
  </what-built>
  <how-to-verify>
    Start dev server (`npm run dev`), hard-refresh http://site.localhost:3000/wrap-studio. Open DevTools Network tab.

    Success Criterion 1 (UPLOAD-01/02/03/04):
    1. Upload a JPG via the button → progress bar appears and advances to 100% → clean masked car appears.
    2. Confirm Network tab shows NO request to an external removal API during removal.
    3. Repeat with a PNG (file picker) and via drag-and-drop.
    4. Upload a .heic file → converts and shows masked car.

    Success Criterion 2 (RCOL-01..08):
    5. Cycle through all 9 finishes (gloss, satin, matte, chrome, shift, metallic, carbon, ppf-clear, ppf-matte). Confirm: gloss amplifies highlights, matte flat, satin dampened, chrome sweeps gradient band, metallic shows subtle grain (vs gloss same hue), shift animates two-tone, ppf thin tint. Each updates instantly on the silhouette only.

    Success Criterion 3 (RCOL-09):
    6. Assign different colours to Bonnet vs Full body via panel chips — each holds independently.

    Success Criterion 4 (RCOL-10):
    7. Click Compare, drag the slider — left shows original photo with real background, right shows wrapped cutout. Confirm Compare button was disabled before any upload.

    Persistence: hard-refresh after upload+selection — car and selection restore (or, if quota exceeded, selection restores and that is acceptable per design).
  </how-to-verify>
  <verify>Human confirms all 4 success criteria and 14 requirements in browser.</verify>
  <done>User types "approved" or lists issues to feed gap-closure.</done>
  <resume-signal>Type "approved" or describe any issues found (which become gap-closure inputs).</resume-signal>
</task>

</tasks>

<verification>
All 4 success criteria and 14 requirements confirmed by human in browser.
</verification>

<success_criteria>
Phase 6 UAT approved, or gaps documented for `/gsd:plan-phase --gaps`.
</success_criteria>

<output>
After completion, create `.planning/phases/06-upload-recolour-engine/06-04-SUMMARY.md`
</output>
