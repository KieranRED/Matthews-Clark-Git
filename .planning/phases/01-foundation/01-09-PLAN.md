---
phase: 01-foundation
plan: 09
type: execute
wave: 6
depends_on: [01-02, 01-05, 01-06, 01-07, 01-08]
files_modified: []
autonomous: false
requirements: [UPLOAD-01, UPLOAD-02, UPLOAD-03, UPLOAD-04, UPLOAD-05, UPLOAD-06, UPLOAD-07, UPLOAD-08, SCHEDULE-01, SCHEDULE-02, SCHEDULE-03, SCHEDULE-04, SCHEDULE-05, PUBLISH-01, PUBLISH-02, PUBLISH-03, PUBLISH-04, PUBLISH-05]
must_haves:
  truths:
    - "User can drag/drop or pick a video and see Optimised ✓ or Check export ⚠ within 10s of upload (UPLOAD-01..05)"
    - "User can submit a post with caption, hashtags, scheduled datetime, Instagram toggle, and optional PDF; the post appears in the queue as Scheduled (SCHEDULE-01..03, UPLOAD-06..07)"
    - "A scheduled post advances to Processing then Published on Instagram, with ig_media_id stored (PUBLISH-01..03, SCHEDULE-04)"
    - "A failed post displays the error reason inline with a Retry button that resets status to pending (SCHEDULE-05)"
    - "Daily blob-cleanup cron deletes videos older than 7 days (UPLOAD-08)"
    - "Daily token-refresh cron renews the IG access token (PUBLISH-04)"
    - "Cron lock prevents double-publish if cron double-fires (PUBLISH-05)"
  artifacts: []
  key_links: []
---

<objective>
Phase 1 end-to-end verification checkpoint. No new code is written in this plan — it confirms that everything built in Plans 01–08 actually works together against the production Vercel deploy.

Purpose: This is the goal-backward verification gate for Phase 1's five Success Criteria from ROADMAP.md. Until this passes, Phase 2 (TikTok) cannot start.

Output: Confirmation from the user that all 18 requirements behave as specified end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: User-acceptance verification of the full upload pipeline (UPLOAD-01..07)</name>
  <read_first>
    - .planning/phases/01-foundation/01-VALIDATION.md (### Per-Task Verification Map — gives exact validation instructions for each UPLOAD req)
    - .planning/phases/01-foundation/01-UI-SPEC.md (Copywriting Contract — confirms exact tag strings expected)
  </read_first>
  <files>(no file changes — human verification only)</files>
  <action>
    No automated action — blocking human verification gate. The executor surfaces the `<how-to-verify>` checklist to the user, pauses for explicit acceptance, and does NOT commit any code changes for this task. If the user reports failures, the executor MUST stop and document the gaps so `/gsd:plan-phase --gaps` can route fixes.
  </action>
  <verify>
    <automated>echo "human-verification — wait for user acceptance signal"</automated>
  </verify>
  <done>User has typed "A pass, B pass, C pass, D pass" (or filed specific GAP items) confirming the upload + creation + queue UI behaves per the four checklist sections.</done>
  <what-built>
    Plans 01–06 delivered the complete upload + post-creation pipeline:
    - Vercel Blob client upload to `social-videos/` and `social-scripts/`
    - mediainfo.js quality check returning Optimised ✓ / Check export ⚠
    - PDF text extraction stored on the post record
    - Post creation form at /admin/content/new
    - Post queue at /admin/content with grouped status sections
  </what-built>
  <how-to-verify>
    Open the deployed Vercel URL in a browser. Sign in via the admin login. Then run through this checklist:

    **A. Navigation (Plan 05)**
    1. Bottom nav shows: Today | Pipeline | Clients | Pricing | Content. Click "Content".
    2. URL is `/admin/content`. TopBar shows `SOCIAL · QUEUE` eyebrow above `Content`.
    3. TopBar right side has three icon buttons (search, bell, gear). Click the gear — you land on `/admin/settings`.
    4. Return to `/admin/content`. Tap the floating + button (bottom-right). URL is now `/admin/content/new`.

    **B. Video upload + quality check (UPLOAD-01..05)**
    5. Drag any .mp4 file onto the dropzone (or tap to pick).
    6. Progress bar fills 0 → 100%. After completion, filename + size appear inline.
    7. Within 10 seconds, the quality tag appears: `Optimised ✓` (green) for a true 1080×1920 H.264 Reel, or `Check export ⚠` (amber) otherwise. While in flight, the tag reads `Checking…`.
    8. Pick a non-Reel video (e.g. 1920×1080 landscape). The tag must read `Check export ⚠`.

    **C. PDF + form submit (UPLOAD-06..07, SCHEDULE-01..02)**
    9. Tap the PDF picker. Pick any .pdf. Filename appears after upload.
    10. Type a caption. Type some hashtags. Pick a future datetime ≥ 5 minutes from now.
    11. Tap `Schedule Post →`. You land back on `/admin/content` with the new post showing in the `SCHEDULED` section.

    **D. Queue layout (SCHEDULE-03)**
    12. Section order top→bottom is `FAILED` → `SCHEDULED` → `PROCESSING` → `PUBLISHED`. Empty sections are not rendered.

    Expected outcomes:
    - Quality tag is correct for both pass and fail videos
    - PDF filename surfaces after upload (server-side text extraction is verified separately in Task 3)
    - Submitted post appears in the queue under SCHEDULED
    - Bottom nav highlights "Content" on both /admin/content and /admin/content/new
  </how-to-verify>
  <acceptance_criteria>
    - All items A.1 → D.12 in the checklist above are confirmed by the user
    - The user explicitly types the outcome of each checklist letter group (A, B, C, D) with PASS/FAIL
    - If any step FAILs: user describes the actual vs expected behavior so /gsd:plan-phase --gaps can route a fix
  </acceptance_criteria>
  <resume-signal>Type "A pass, B pass, C pass, D pass" (or list specific failures with screenshots/curl output).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User-acceptance verification of the posting cron + IG state machine (SCHEDULE-04, PUBLISH-01..03, PUBLISH-05)</name>
  <read_first>
    - .planning/phases/01-foundation/01-VALIDATION.md (### Per-Task Verification Map — SCHEDULE-04 + PUBLISH-01..05)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pitfall 5: Instagram API Rate Limit — to interpret 400 responses)
  </read_first>
  <files>(no file changes — human verification only)</files>
  <action>
    No automated action — blocking human verification gate. The executor surfaces the `<how-to-verify>` checklist to the user, pauses for explicit acceptance, and does NOT commit any code changes for this task. If the user reports failures, the executor MUST stop and document the gaps so `/gsd:plan-phase --gaps` can route fixes.
  </action>
  <verify>
    <automated>echo "human-verification — wait for user acceptance signal"</automated>
  </verify>
  <done>User has confirmed the cron lock test produced lock-held, a real Reel was published to Instagram with igMediaId stored, and the state machine transitioned pending → processing → published end-to-end.</done>
  <what-built>
    Plan 07 delivered:
    - `vercel.json` with the three cron schedules
    - `lib/igPublish.js` with createIgContainer / pollIgContainer / publishIgContainer / acquireCronLock / getIgAccessToken
    - `/api/cron/post` handler that walks the two-phase state machine
  </what-built>
  <how-to-verify>
    **Prerequisites — confirm before running this checklist:**
    - Vercel Pro plan is active (cron settings page shows */15 enabled)
    - `CRON_SECRET`, `IG_ACCESS_TOKEN`, `IG_USER_ID`, `BLOB_READ_WRITE_TOKEN` are set in Vercel project env (all environments)
    - The Instagram account is a Business account connected to a Facebook Page
    - At least one post is in the queue at SCHEDULED with `scheduledAt` in the past or near future

    **Smoke tests (run from your terminal — replace placeholders):**

    1. **PUBLISH-05 (lock)** — Fire the posting cron TWICE in quick succession:
       ```bash
       curl -s -H "Authorization: Bearer $CRON_SECRET" https://YOUR_URL/api/cron/post &
       curl -s -H "Authorization: Bearer $CRON_SECRET" https://YOUR_URL/api/cron/post &
       wait
       ```
       At least one response must contain `"skipped":true` and `"reason":"lock-held"`.

    2. **SCHEDULE-04 + PUBLISH-01** — Create a post in the UI with `scheduledAt` ~1 minute in the past, OR manually fire the cron after creating one. Then check Upstash console (or hit `GET /api/admin/content/{id}`) — confirm `status: "processing"` and `igContainerId` is a non-empty string.

    3. **PUBLISH-02 + PUBLISH-03** — Wait ~30 seconds (IG container takes time to FINISH for Reels). Fire the cron again:
       ```bash
       curl -s -H "Authorization: Bearer $CRON_SECRET" https://YOUR_URL/api/cron/post | head -c 600
       ```
       The response `details[]` array should show `action: "still-processing"` (if not finished) or `action: "published"` with an `igMediaId`. Re-fire every minute until you see `published`.

    4. **End-to-end on Instagram** — Open the Instagram profile in a browser. The Reel must appear in the Reels tab with the caption + hashtags from the post creation form.

    Expected outcomes:
    - The cron lock test produces exactly one `skipped:true` response when two crons fire in parallel
    - A pending post visibly transitions: `pending` → `processing` (igContainerId set) → `published` (igMediaId set)
    - The Reel is visible on the actual Instagram account
    - If the IG token is invalid or IG_USER_ID is wrong, the post lands in `failed` with `igError` set — verify the UI shows this and offers Retry
  </how-to-verify>
  <acceptance_criteria>
    - User confirms the lock test produced `{ skipped: true, reason: 'lock-held' }` on at least one of two concurrent requests
    - User confirms a real Reel was published to the connected Instagram account
    - User confirms the `igMediaId` is stored on the post record in KV
    - If any failure occurred: user describes the `igError` text + status_code from the cron response
  </acceptance_criteria>
  <resume-signal>Type "Lock pass, IG container created, IG published, Reel visible on IG" with the post ID used. If anything failed, paste the `details[]` array from the failing cron response.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User-acceptance verification of retry + housekeeping crons + PDF text storage (SCHEDULE-05, UPLOAD-07, UPLOAD-08, PUBLISH-04)</name>
  <read_first>
    - .planning/phases/01-foundation/01-VALIDATION.md (### Per-Task Verification Map — SCHEDULE-05, UPLOAD-07, UPLOAD-08, PUBLISH-04)
  </read_first>
  <files>(no file changes — human verification only)</files>
  <action>
    No automated action — blocking human verification gate. The executor surfaces the `<how-to-verify>` checklist to the user, pauses for explicit acceptance, and does NOT commit any code changes for this task. If the user reports failures, the executor MUST stop and document the gaps so `/gsd:plan-phase --gaps` can route fixes.
  </action>
  <verify>
    <automated>echo "human-verification — wait for user acceptance signal"</automated>
  </verify>
  <done>User has confirmed scriptText populated, Retry resets a failed post to pending, token-refresh wrote new KV values, and blob-cleanup returned the expected JSON shape — and has explicitly typed "Phase 1 approved" or listed specific GAPs.</done>
  <what-built>
    Plans 03, 04, 05, 08 delivered:
    - Server-side PDF text extraction via unpdf (Plan 03 + 04)
    - Retry button on failed cards → PATCH /api/admin/content/[id] (Plan 04 + 05)
    - /api/cron/token-refresh (Plan 08)
    - /api/cron/blob-cleanup (Plan 08)
  </what-built>
  <how-to-verify>
    **A. PDF text extraction (UPLOAD-07)**
    1. After creating a post with a PDF (from Task 1, step C.9), find the post id in the queue or Upstash.
    2. Hit `GET /api/admin/content/{id}` (with admin cookie) — the response's `post.scriptText` must be a non-empty string containing recognizable text from the PDF.

    **B. Retry (SCHEDULE-05)**
    3. Manually mark a post as `failed` either by: (a) using a bad video URL so the IG container creation fails on the next cron run, OR (b) directly editing the KV record to `status: "failed", igError: "test"`.
    4. Refresh `/admin/content` — the failed card shows under the `FAILED` section with the error text and a `Retry Post` button.
    5. Click `Retry Post`. The card optimistically moves to `SCHEDULED`. After the page refreshes, the post is back in pending status (verified via KV or the next cron pickup).

    **C. Token refresh (PUBLISH-04)**
    6. From terminal:
       ```bash
       curl -s -H "Authorization: Bearer $CRON_SECRET" https://YOUR_URL/api/cron/token-refresh
       ```
       Response is `{ ok: true, refreshedAt, expiresAt }` with `expiresAt` ~60 days in the future.
       7. Check Upstash console — `ig:access_token` value has changed and `ig:token_expires_at` matches the response.

    **D. Blob cleanup (UPLOAD-08)**
    8. From terminal:
       ```bash
       curl -s -H "Authorization: Bearer $CRON_SECRET" https://YOUR_URL/api/cron/blob-cleanup
       ```
       Response is `{ ok: true, scanned: N, deleted: M, cutoff }`. For a fresh project with no blobs older than 7 days, `deleted: 0` is correct.
    9. (Optional) Confirm a 7+ day old blob actually gets removed: in Vercel Blob dashboard, manually create a blob with an old uploadedAt (or wait for an existing one to age) — re-run the cleanup, and verify the blob is gone.

    Expected outcomes:
    - `scriptText` is populated on the test post
    - Retry visibly moves a failed card back to SCHEDULED
    - Both daily crons respond 200 with the expected JSON shape
    - Token refresh writes new values to KV
  </how-to-verify>
  <acceptance_criteria>
    - User confirms A.2 (`scriptText` non-empty)
    - User confirms B.5 (retry moves card and resets status)
    - User confirms C.6 + C.7 (token-refresh response + KV write)
    - User confirms D.8 (blob-cleanup response)
    - User explicitly approves Phase 1 as complete (or files specific gaps for `/gsd:plan-phase --gaps`)
  </acceptance_criteria>
  <resume-signal>Type "Phase 1 approved — A pass, B pass, C pass, D pass". If any gap exists, list it as "GAP: {req-id} {description}" so it can be routed to gap closure.</resume-signal>
</task>

</tasks>

<verification>
- All 18 Phase 1 requirements explicitly tested via the three checklists
- The 5 ROADMAP Success Criteria for Phase 1 are confirmed:
  1. Upload + quality check within 10s ✓ (Task 1 B.5–B.8)
  2. Post creation with all fields → queue as Scheduled ✓ (Task 1 C.10–C.12)
  3. Scheduled → Processing → Published on Instagram with ig_media_id ✓ (Task 2 #2–#4)
  4. Failed post with retry ✓ (Task 3 B.3–B.5)
  5. Blob TTL cleanup ✓ (Task 3 D.8)
</verification>

<success_criteria>
- User has run through all three checklists and either approved or filed specific GAP items
- A real Reel has been published to the M&C Instagram account via the cron
- The system can be left running unattended (cron locks, token refresh, blob cleanup all verified)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-09-SUMMARY.md` documenting:
- The Vercel URL used for verification
- The post id used to verify the end-to-end IG publish
- The igMediaId returned by the publish step
- Any gaps surfaced for `/gsd:plan-phase --gaps` to address
- Final approval signature from the user
</output>
