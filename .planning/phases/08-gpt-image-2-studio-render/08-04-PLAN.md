---
phase: 08-gpt-image-2-studio-render
plan: 04
type: execute
wave: 4
depends_on: ["08-03"]
files_modified: []
autonomous: false
requirements: [RENDER-04, RENDER-06]

must_haves:
  truths:
    - "An end-to-end studio render produces a car composited into a studio bay scene"
    - "The before/after slider compares original photo vs studio render with correct tags"
    - "Rate-limit blocking and error toasts behave as specified"
  artifacts: []
  key_links:
    - from: "browser /wrap-studio"
      to: "rendered studio image"
      via: "Studio Render button -> /api/wrap-render -> renderUrl on stage"
      pattern: "manual"
---

<objective>
Human verification of the actual GPT-Image-1 render result and end-to-end flow. Automated greps cannot judge render quality (RENDER-04) or confirm the live before/after comparison (RENDER-06) — a human must run the real flow.

Purpose: Sign off RENDER-04 (studio bay integration) and RENDER-06 (render replaces preview, BA slider) with eyes on the actual output.
Output: Approval or a defect list feeding a gap-closure plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-gpt-image-2-studio-render/08-UI-SPEC.md
@.planning/phases/08-gpt-image-2-studio-render/08-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify live studio render end-to-end</name>
  <read_first>.planning/phases/08-gpt-image-2-studio-render/08-UI-SPEC.md</read_first>
  <what-built>
    /api/wrap-render route (gpt-image-1 images.edit, rate-limited), window.__wrapRenderCanvas helper,
    real startRender fetch, renderUrl state + BA slider wiring, updated veil copy.
  </what-built>
  <action>Human-only: the executor cannot judge render quality or operate the live UI. Pause and present the verification steps below; resume on the signal.</action>
  <how-to-verify>
    Ensure OPENAI_API_KEY is set locally, then `npm run dev` and open `/wrap-studio`.
    1. Upload a car photo and select a colour + finish (e.g. gloss).
    2. Click "Studio Render" (HUD top-right).
       - Expect: render veil appears, eyebrow "Studio Render", heading "Rendering studio shot…",
         body "Compositing into the M&C studio bay — usually 15–30 seconds.", percentage with NO time suffix.
       - Expect: progress bar creeps slowly (does not freeze at 90% before ~45s), then jumps to 100%.
    3. On success: toast "Studio render ready". Inspect the result image:
       - Expect (RENDER-04): the car is composited into a studio bay scene with studio lighting / floor reflection.
       - Expect: the wrap colour + finish character is preserved (GPT did not re-colour).
    4. Toggle the before/after slider:
       - Expect (RENDER-06): after side = studio render, before side = original photo.
       - Expect tags: "Studio Render" (after) / "Original" (before).
    5. Button now reads "Re-render".
    6. Error/limit checks:
       - Trigger 4 renders in one session → 4th shows "Too many renders — try again shortly".
       - (Optional) temporarily set DAILY_CAP low or unset KV to confirm 429 path copy.
       - Force an API error (e.g. invalid key) → toast "Render failed — try again", renderUrl stays null, CSS preview intact.
  </how-to-verify>
  <acceptance_criteria>
    - Render output shows car in a studio scene (RENDER-04) — human judgment
    - BA slider compares original vs render with correct tags (RENDER-06)
    - Veil copy matches UI-SPEC exactly
    - Distinct toasts confirmed for success / rate-limit / API error
  </acceptance_criteria>
  <resume-signal>Type "approved" or list defects (these become a gap-closure plan)</resume-signal>
</task>

</tasks>

<verification>
Human confirms render quality, BA comparison, copy, and toast behaviour against UI-SPEC.
</verification>

<success_criteria>
RENDER-04 and RENDER-06 visually confirmed in a live render; all toast/copy contracts met.
</success_criteria>

<output>
After completion, create `.planning/phases/08-gpt-image-2-studio-render/08-04-SUMMARY.md`
</output>
