---
phase: 08-gpt-image-2-studio-render
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .env.example
  - lib/kv.js
  - public/wrap-studio/studio-bay.jpg
autonomous: false
requirements: []
user_setup:
  - service: openai
    why: "GPT-Image-1 studio render via images.edit()"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI Dashboard -> API keys (https://platform.openai.com/api-keys)"
    dashboard_config:
      - task: "Confirm OPENAI_API_KEY is set in Vercel project env (Production + Preview)"
        location: "Vercel -> Project -> Settings -> Environment Variables"

must_haves:
  truths:
    - "openai package is importable in server route code"
    - "OPENAI_API_KEY is documented in .env.example and present in .env.local"
    - "kvExpire helper exists for setting a daily TTL on the rate-limit counter"
    - "A studio bay background asset exists in public/wrap-studio/"
  artifacts:
    - path: "package.json"
      provides: "openai dependency"
      contains: "\"openai\""
    - path: ".env.example"
      provides: "OPENAI_API_KEY documentation"
      contains: "OPENAI_API_KEY"
    - path: "lib/kv.js"
      provides: "kvExpire(key, seconds) helper"
      contains: "export async function kvExpire"
    - path: "public/wrap-studio/studio-bay.jpg"
      provides: "Studio bay background for compositing reference"
  key_links:
    - from: "lib/kv.js kvExpire"
      to: "Upstash REST EXPIRE endpoint"
      via: "kvFetch('/expire/{key}/{seconds}')"
      pattern: "kvFetch\\(`/expire"
---

<objective>
Wave 0 infrastructure: install the `openai` package, document/seed `OPENAI_API_KEY`, add a `kvExpire` helper to `lib/kv.js` for the rate-limit TTL, and add a studio bay background asset. No route or UI code can compile or run without this.

Purpose: Unblock plan 08-02 (route) and 08-03 (wiring). The `openai` package is NOT in package.json and `OPENAI_API_KEY` is undefined — both are hard blockers.
Output: openai dependency installed, env var documented + seeded, kvExpire export, studio-bay.jpg asset.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-gpt-image-2-studio-render/08-RESEARCH.md
@.planning/phases/08-gpt-image-2-studio-render/08-CONTEXT.md

<interfaces>
From lib/kv.js (existing exports — do not modify their signatures):
```javascript
export function hasKv()
export async function kvFetch(path, { method = "GET", body } = {})   // Upstash REST passthrough
export async function kvGet(key)
export async function kvSet(key, value)                              // NO TTL parameter
export async function kvIncr(key)
```
kvFetch builds the Upstash REST URL from path. The EXPIRE endpoint is `/expire/{key}/{seconds}`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install openai package and document OPENAI_API_KEY</name>
  <read_first>package.json, .env.example</read_first>
  <files>package.json, .env.example, .env.local</files>
  <action>
    Run `npm install openai` (installs openai ^4.x into package.json dependencies and package-lock.json).
    Verify the installed version with `npm view openai version` and confirm package.json now lists "openai".

    Add to `.env.example` (append a new block):
    ```
    # OpenAI (used by /api/wrap-render for GPT-Image-1 studio renders)
    OPENAI_API_KEY=
    ```

    Add `OPENAI_API_KEY=` line to `.env.local` if the file exists (leave value blank — user fills it, flagged via user_setup frontmatter). If `.env.local` does not exist, do not create it; the user_setup checkpoint covers seeding.

    Do NOT hardcode any key value anywhere.
  </action>
  <verify>
    <automated>node -e "require('openai'); console.log('ok')" && grep -q "OPENAI_API_KEY" .env.example && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"openai"' package.json` returns a match
    - `grep OPENAI_API_KEY .env.example` returns a match
    - `node -e "require('openai')"` exits 0 (package resolvable)
  </acceptance_criteria>
  <done>openai is in package.json and require()-able; OPENAI_API_KEY documented in .env.example.</done>
</task>

<task type="auto">
  <name>Task 2: Add kvExpire helper to lib/kv.js</name>
  <read_first>lib/kv.js</read_first>
  <files>lib/kv.js</files>
  <action>
    Add a new export to `lib/kv.js`, placed after the existing `kvIncr` function:
    ```javascript
    // Set a TTL (seconds) on an existing key via Upstash REST EXPIRE.
    export async function kvExpire(key, seconds) {
      if (!hasKv()) return null;
      return kvFetch(`/expire/${encodeURIComponent(key)}/${seconds}`, { method: "POST" });
    }
    ```
    Do not alter any existing export. Reuse the existing `hasKv` and `kvFetch` functions already defined in the file.
  </action>
  <verify>
    <automated>grep -q "export async function kvExpire" lib/kv.js && grep -q "/expire/" lib/kv.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export async function kvExpire" lib/kv.js` returns a match
    - `grep "kvFetch(\`/expire/" lib/kv.js` returns a match
    - No existing export signature changed (kvFetch, kvIncr, kvGet still present)
  </acceptance_criteria>
  <done>kvExpire(key, seconds) exported, calls Upstash EXPIRE via kvFetch.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Provide studio bay background asset + confirm API key</name>
  <what-built>Env var documented; kvExpire helper added; openai installed.</what-built>
  <action>Human-only: the executor cannot create accounts, set secrets, or source/photograph a studio image. Pause and present the verification steps below; resume on the signal.</action>
  <how-to-verify>
    Two human-only items remain (no CLI/API can do these):
    1. Place a 1536x1024 studio bay background image at `public/wrap-studio/studio-bay.jpg`.
       If you do not have one yet, that is fine — the route in plan 08-02 will run prompt-only as documented. Reply "no asset" to proceed prompt-only.
    2. Confirm `OPENAI_API_KEY` is set:
       - locally in `.env.local` (paste your sk-... key)
       - in Vercel project env (Settings -> Environment Variables) for Production + Preview
  </how-to-verify>
  <acceptance_criteria>
    - User confirms OPENAI_API_KEY is seeded locally and in Vercel
    - User either provides studio-bay.jpg OR explicitly approves prompt-only mode
  </acceptance_criteria>
  <resume-signal>Type "key set" plus either "asset added" or "no asset"</resume-signal>
</task>

</tasks>

<verification>
- `node -e "require('openai')"` exits 0
- `.env.example` contains OPENAI_API_KEY
- `lib/kv.js` exports kvExpire
- User has confirmed key is set and asset decision made
</verification>

<success_criteria>
openai installed and importable; OPENAI_API_KEY documented; kvExpire helper present; studio bay asset provided or prompt-only approved.
</success_criteria>

<output>
After completion, create `.planning/phases/08-gpt-image-2-studio-render/08-01-SUMMARY.md`
</output>
